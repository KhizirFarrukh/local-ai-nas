# Session S006: 2026-09-25

| Field | Value |
|---|---|
| Session number | S006 |
| Date | 2026-09-25 |
| Start time | 00:23 +0500 (system clock at R1: 2026-09-25 00:23:12 +0500) |
| Editor | Claude Code (VS Code extension), Windows 11 |
| Model | Claude Opus 5.5 (`claude-opus-5-5`) |
| Plan version at start | 1.1.4 |
| Active stage / task at start | none (S01 Done; S02 not planned yet) |

## Startup

- **Documents read:** this session continues the same conversation, right after S005 was closed, so RULES.md, CURRENT_STATE.md, plan.md (sections 9, 12, and the S01/S02 sections), the S005 log, and the S01 stage document are current in context. For this request, RULES R6, plan sections 9 and 12, ADR-0005, the stage template, `docs/testing.md`, the README, and `.github/workflows/ci.yml` were re-read.
- **Previous session ended cleanly?** Yes: S005 has its closing summary (E128), and every branch is merged into `develop`.
- **Git state:** `develop` at `89c2c2e`, clean and in sync with `origin/develop`.
- **Resume summary given to the user:** S01 is done and signed off. The next planned action is the S02 (NAS GUI) stage document, and nothing was open. The user's new message changes how tests are written, starting with S02.

## Entries

### E001 | 00:23 | USER
- The user's message, verbatim: "Make sure to add unit tests and integration tests and system/ application tests a part of development process but focus on coding first, write code that is testable, but write tests in the end (of the stage)"
- This is a lasting instruction about the development process (R10). It changes RULES R6, which currently says "Write tests alongside or before the code (test-first where practical). No task is complete without its tests." and "When a bug is found, record it, write a failing test that reproduces it, then fix it."

### E002 | 00:30 | IMPACT ANALYSIS (R4)
- **Affected:**
  - RULES R6 (the two bullets above) and User Preferences.
  - Plan section 9: "Tests written with or before the code".
  - Plan section 12 (the testing substages and test levels): "system/application tests" become a named level.
  - `templates/stage-template.md`: the task rules, section 8 "Commands … before a task is marked Done", and the final substage's test tasks.
  - `docs/testing.md` ("Every task ships its tests"; the coverage gate) and the README checks table.
  - **CI:** the coverage step fails every push below 80% of `internal/...`. With tests written at the stage end, untested code merged during a stage lowers the total and could turn CI red.
- **Not affected:**
  - ADR-0005 names the tools only; it sets no timing and no threshold.
  - The S01 stage is done. S02 has no stage document yet, so it can be written the new way from the start.
- The impact is significant (the process for every stage), and two details are open. Per R4 step 3, the user is asked to confirm before editing.

### E003 | 01:23 | USER DECISIONS (testing approach details)
- Asked two questions (AskUserQuestion) after the impact summary. The user's answers, verbatim:
  1. "CI currently fails any push when test coverage drops below 80%. With tests written at the end of a stage, what should the coverage check do?" → **"Report now, enforce at stage end (Recommended)"**. The option text: "Every push still shows the coverage figure, but a low figure doesn't block merging during the stage. Reaching 80% becomes an exit criterion of each stage's final testing substage."
  2. "When I find a bug while coding, when should its regression test be written?" → **"Fix now, test at stage end (Recommended)"**. The option text: "Record the bug in the log and stage document and fix it. Its regression test is written with the stage's other tests, working from that record so none is forgotten."

### E004 | 01:23 | PLAN + CHANGE: testing approach (RULES 1.6.0, plan 1.2.0)
- Branch `docs/S006-testing-approach` off `develop`.
- **RULES 1.5.0 → 1.6.0:** R6's test bullets rewritten (code first and testable; the stage's final testing substage writes unit, integration, and system/application tests; bug regression tests from the record). A User Preference records the instruction and both answers. Changelog row added.
- **Plan 1.1.4 → 1.2.0** (MINOR, R4: it changes how every stage is developed and what its final substage contains; 1.1.4 archived):
  - 2b principle 6 and section 9 (engineering, stage end, coverage);
  - 12.1: the final substage writes the stage's tests, including the tests that earlier substages name;
  - 12.2: "System / application" replaces "End-to-end" as a test level;
  - revision entry.
- **Stage template:**
  - tasks deliver testable code;
  - "Done" means build, lint/format, the existing tests, and a run;
  - the final substage gets separate unit and integration/system test tasks (T01, T02) before the documentation, audit, and completion tasks;
  - section 8 and section 9 updated.
- **`docs/testing.md`:** the approach, the system/application level, and the coverage rule.
- **CI:** the coverage step gets `continue-on-error: true` and a new name, so it still reports on every push but does not block.
- **README:** unchanged. Its checks table stays accurate (the script still requires 80% when run), and README changes outside a stage's sections need a proposal (A002 F-005).
- Existing tests keep running on every push and must stay green. When a change breaks one, the code or the test is fixed in that task.

### E005 | 01:23 | CORRECTION (timestamps of E001 and E002)
- **E002's time "00:30" was typed by hand, not read from the system clock** (R2). Both E001 and E002 were written in the same step, when the log file was created, and the file's creation time is **00:24:35**. E001's 00:23 is the startup clock reading, so it is correct; E002's real time is 00:24.
- The gap to E003 (01:23) was the time the user took to answer the two questions.
- Entry times are taken from `date` from here on.

### E006 | 01:30 | USER + TASK START: S02 stage document (R3)
- Testing approach change: CI run 36054572413 green; merged into `develop` (bdc9832).
- The user's message, verbatim: "Continue". The next step in CURRENT_STATE is to plan S02 (NAS GUI) in detail.
- Branch `docs/S02-stage-plan` off `develop`. **Plan:**
  - Read the plan's S02 section (10.3), its requirements, the open questions S02 needs, ADR-0009 (SvelteKit) and the related ADRs, and the web dependencies (register section 4).
  - Write `stages/S02-nas-gui.md` from the stage template (R3 sections 1–13), with S02.1–S02.7 as code tasks and S02.8 as the test substage (R6, S006).
  - Present it to the user with the decisions it needs. No S02 code before approval.

### E007 | 01:38 | S02 STAGE DOCUMENT WRITTEN (R3)
- **Read:** plan 10.3 (S02.1–S02.8, design notes, exit criteria), the S02 requirements (FR-002–007, FR-021, FR-077–083, NFR-001, NFR-014, NFR-015, NFR-022, NFR-027), RK-19, ADR-0009, ADR-0004 (`web/embed.go`), register section 4, `internal/files/list.go`, and the S01 guard tests.
- **Checked on the npm registry (2026-09-25):**
  - Every web package is at its registered version, except vite, now 8.3.1.
  - The peer ranges keep **TypeScript pinned to 6.0.3**: svelte-check 4.7.6 and @sveltejs/kit 2.70.3 accept `^6`, and typescript-eslint 8.70.1 accepts `<6.1.0`.
  - New packages for the plan: @lucide/svelte 1.48.0 (ISC), @sveltejs/vite-plugin-svelte 7.3.1, @eslint/js 10.0.1, globals 17.12.0, @vitest/browser-playwright 5.0.1, vitest-browser-svelte 3.1.0, and @vitest/coverage-v8 5.0.1.
  - CI actions: actions/setup-node v7.0.0 and pnpm/action-setup v6.1.0. The Node.js LTS is 24.21.0; 24.19.0 is installed here. pnpm is not installed yet.
- **`stages/S02-nas-gui.md`**, all sections 1–13:
  - 29 tasks: S02.1–S02.7 deliver code, and S02.8 has the tests, cross-browser report, guide, audit A003, and sign-off.
  - Server additions: serving the embedded app with security headers; `total` and `offset` in listings (`readFolder` already reads and sorts the whole folder); streamed ZIP archives through a JSON ticket and a GET download.
  - Design points: the embed placeholder (`web/build/.gitkeep`, so `go build` works without Node.js), headless Uppy with resume, the safe preview rules, and pdf.js recommended.
  - Guard tests from S01 are kept green by adding the entries they require (R6).
  - The README is changed only in its Development section, which the files table names (A002 F-005).
- **Plan:** S02 status "Not started" → **Planned** in 10.1 and 10.3. A status update, so no version bump (the 1.1.4 precedent).
- **Next:** ask the user to approve the document, with decision 2 (real Safari and phones).

### E008 | 01:38 | CORRECTION + APPROVAL ASKED
- **Correction to E007:** the stage document has **33 tasks**, not 29 (S02.1: 5, S02.2: 3, S02.3: 4, S02.4: 4, S02.5: 4, S02.6: 5, S02.7: 3, S02.8: 5). Counted with `grep` on the task rows.
- The document is committed on `docs/S02-stage-plan` and pushed. The user is asked to approve it, together with decision 2 (section 11).

### E009 | 01:44 | USER APPROVAL S02 + DECISION (Safari)
- The user's answers (AskUserQuestion), verbatim:
  1. "Do you approve the S02 (NAS GUI) stage document, so that S02.1-T01 (install pnpm, create the web project) can start?" → **"Approve S02 (Recommended)"**
  2. "Until stage 3, a phone or a Mac can't reach the server. How should Safari and real phones be checked?" → **"Skip Safari"**. The option text: "Chrome, Edge, and Firefox only. Safari is not checked."
- **Applied:**
  - `stages/S02-nas-gui.md`: **Approved**; approval record quoted. S02.8's cross-browser work covers Chrome, Edge, and Firefox. Playwright runs Chromium, Firefox, and Edge (Windows); no WebKit. The NFR-027 row, section 9, section 10, and the test plan are updated, with a change-log row.
  - **Plan 1.2.0 → 1.3.0** (MINOR, R4: a substage acceptance criterion changed at the user's decision; 1.2.0 archived): S02.8 criterion 2 now names Chrome, Edge, and Firefox, and says Safari is not checked in S02. S02 status is Approved.
  - Not changed: NFR-027 and the Safari checks planned for later stages (for example S04.9 video on Safari). The decision was about S02.

### E010 | 01:50 | TASK START S02.1-T01 (toolchain and project) + PLAN
- The S02 stage document: CI run 36056943504 green; merged into `develop`. Branch `feat/S02.1-T01-toolchain`.
- **Plan (stage document S02.1-T01):**
  1. **Development PC:**
     - install pnpm 12.6.0 with `npm install -g pnpm@12.6.0` (per-user npm prefix, no admin rights);
     - upgrade Node.js with `winget upgrade --id OpenJS.NodeJS.LTS` (24.19.0 → 24.21.0). If Windows asks for admin rights and the upgrade cannot finish, 24.19.0 stays (it is in the same LTS line, and `engines` is `>=24.19`); that is recorded.
  2. **Create `web/` by hand** rather than with `sv create`, so every file is known and every version is pinned exactly:
     - `package.json` (`engines`, `packageManager: pnpm@12.6.0`, scripts `dev`, `build`, `preview`, `check`, `lint`, `format`, `format:check`, `generate`);
     - `svelte.config.js` (adapter-static with the `index.html` fallback; `kit.csp` hash mode);
     - `vite.config.ts` (Tailwind plugin; dev proxy `/api` → 127.0.0.1:8080);
     - `tsconfig.json`, `eslint.config.js`, `.prettierrc`, `.prettierignore`, `.npmrc`;
     - `src/app.html`, `src/app.css`, `src/routes/+layout.ts` (`ssr = false`, `prerender = false`), `+layout.svelte`, `+page.svelte` (a minimal page).
  3. `pnpm install` → `pnpm-lock.yaml`. `.gitignore`: `web/node_modules`, `web/.svelte-kit`, and `web/build/*` except `.gitkeep`.
  4. Register every direct package (R6) in `dependencies.md`, with pnpm and Node.js in sections 1 and 12 (from-source only). Check every license against `scripts/allowed-licenses.txt` (`pnpm licenses list`).
  5. Check: `pnpm install --frozen-lockfile`, `pnpm lint`, `pnpm check`, and `pnpm build` on Windows, and on Linux (WSL, from a copy outside /mnt/c so the node_modules are Linux's own). The Go checks stay green.

### E011 | 06:17 | USER + TASK DONE S02.1-T01 (toolchain and project)
- **The user's message during the task, verbatim:** "Keep working until this stage is complete" (sent twice). This is the instruction for the rest of S02: continue task by task, without stopping between tasks, until S02 is complete. The user's own decisions (for example the sign-off in S02.8-T05) are still asked. Stacked branches are used while CI runs (User Preferences).
- **Development PC:**
  - `npm install -g pnpm@12.6.0` (per-user prefix): pnpm 12.6.0.
  - `winget upgrade --id OpenJS.NodeJS.LTS --version 24.21.0` failed: "No version found matching" (exit -1978335209). winget's newest is 24.19.0, which is installed, so Windows stays on 24.19.0.
  - In WSL: the official Node.js 24.21.0 Linux tarball in `~/.local/opt/node` (SHA-256 checked against SHASUMS256.txt) and pnpm 12.6.0, no sudo.
- **`web/`**, created by hand:
  - `package.json` (exact versions; `engines` `>=24.19.0 <25`; `packageManager`; scripts);
  - `.npmrc` (save-exact, engine-strict); `pnpm-workspace.yaml` (commented);
  - `svelte.config.js` (adapter-static, `index.html` fallback); `vite.config.ts` (Tailwind, dev proxy `/api`);
  - `tsconfig.json`, `eslint.config.js` (flat, `defineConfig`), `.prettierrc`, `.prettierignore`;
  - `src/app.html` (no inline style, for the coming CSP), `app.css`, `app.d.ts`, `routes/+layout.ts` (`ssr = false`), `+layout.svelte`, `+page.svelte`;
  - `static/favicon.svg`; `build/.gitkeep`.
- **Installed:**
  - dependencies: svelte 5.57.1, @sveltejs/kit 2.70.3, tailwindcss 4.3.3;
  - devDependencies: adapter-static 3.0.10, vite-plugin-svelte 7.3.1, vite 8.3.1, @tailwindcss/vite 4.3.3, typescript 6.0.3, svelte-check 4.7.6, eslint 10.11.0, @eslint/js 10.0.1, eslint-plugin-svelte 3.23.0, typescript-eslint 8.70.1, globals 17.12.0, prettier 3.9.9, prettier-plugin-svelte 4.1.1.
  - pnpm's minimum-release-age guard excluded vite 8.3.1 by name (published less than a day before).
- **Results:**
  - Windows: `pnpm format:check` OK, `pnpm lint` OK, `pnpm check` 0 errors and 0 warnings (178 files), `pnpm build` wrote `build/`.
  - Linux (WSL, a copy in /tmp): `pnpm install --frozen-lockfile`, format, lint, check, and build all OK.
  - Go: build, vet, and golangci-lint 0 issues; `go list ./...` has no package under `web/`. The one `.go` file in node_modules (`flatted`) sits under the dot directory `.pnpm`, which Go ignores.
- **Licenses** (`pnpm licenses list`): all 166 packages are on the allow-list except BlueOak-1.0.0 (`minimatch`, dev only). The T05 check will handle it.
- **Register:** Node.js and pnpm (installed), the section 4 rows marked installed, vite 8.3.1, new rows for vite-plugin-svelte and the ESLint pieces, and a license-scan note.
- **Plan:** S02 and S02.1 set to In Progress. **Stage document:** T01 Done, plus a change-log row with the details.

### E012 | 06:18 | BUG S02-B01 (build deleted web/build/.gitkeep) + FIX
- **Found:** after the T01 commit, `web/build/.gitkeep` was not in it. `vite build` (adapter-static) empties the output directory, so the placeholder that `.gitignore` keeps was deleted before `git add`.
- **Fixed** (R6, S006: record, fix now, test at the stage end): the `build` script in `web/package.json` is now `vite build && node -e "require('node:fs').writeFileSync('build/.gitkeep', '')"`. After `pnpm build`, `build/` holds `.gitkeep`, `_app/`, `favicon.svg`, and `index.html`, and git adds only `.gitkeep`.
- Recorded in the stage change log as **S02-B01**, with its regression test for S02.8.

### E013 | 06:23 | TASK DONE S02.1-T02 (embedding and serving)
- Branch `feat/S02.1-T02-serving`, stacked on `feat/S02.1-T01-toolchain` while T01's CI runs (User Preferences, stacked branches).
- **Code:**
  - **`web/embed.go`** (package `web`, `//go:embed all:build`, `Build()` returns an `fs.FS`).
  - **`internal/webapp`**: `New` reads the build once, keeping an ETag (SHA-256) and a media type from its own table per file, so the Windows registry cannot turn `.js` into `text/plain`. It takes the page's CSP from SvelteKit's `<meta>` and adds `frame-ancestors 'none'`. `ServeHTTP`:
    - allows GET and HEAD only (other methods get `405` with `Allow`);
    - sends `nosniff`, `Referrer-Policy: no-referrer`, and `X-Frame-Options: DENY`;
    - caches `_app/immutable/*` for a year and everything else with `no-cache` plus the ETag;
    - never serves hidden files (`.gitkeep`) and answers missing assets with `404`;
    - falls back to `index.html` for app routes;
    - serves a notice with a strict CSP when the build holds no `index.html`.
  - **`api.Options.App`** and **`withApp`**: every path outside `/api` goes to the app, so `/api/...` keeps its 404 and 405 problems. `serve` builds the app handler and logs a WARN when the interface is not built.
  - **`web/svelte.config.js`**: `kit.csp` in hash mode (self only; `data:`/`blob:` for images and media; `object-src 'none'`; `base-uri` and `form-action` self).
  - **Docs:** `docs/api/conventions.md` and `versioning.md` now say that paths outside `/api` are the web interface.
- **Checked by running** (S02.1-T02 acceptance):
  - With a binary built after `pnpm build` (127.0.0.1:18090):
    - `/` answers 200 `text/html` with the CSP (the SvelteKit hash plus frame-ancestors), nosniff, Referrer-Policy, X-Frame-Options, `no-cache`, and an ETag. `If-None-Match` gives `304`.
    - The deep link `/files/docs` answers 200 with the page. An immutable asset answers 200 `text/javascript` with the year-long cache.
    - `/_app/immutable/nope.js` and `/.gitkeep` answer 404. `/api/nope` is a 404 problem (`application/problem+json`). `POST /` answers 405.
  - **Offline render:** headless Edge with every host but 127.0.0.1 unresolvable rendered `/` ("local-ai-nas", "The web interface is being built") and, at `/files/docs`, the app's own 404 page (routes come in S02.2). So the scripts ran under the CSP with no outside host. The external URLs found in the build are text, not requests: the SVG namespace, Svelte's error-help links, and Tailwind's license comment.
  - **Without the UI:** with the build output moved aside, `go build` still worked, and `/files/x` served the notice with `default-src 'none'`. The server logged the WARN. The build output was restored afterwards.
  - Server log: no ERROR.
- **Checks:** Go build, vet, golangci-lint 0 issues, and the existing `go test ./...` all OK. Web: format, lint, and check (0 errors) OK. The route-inventory guard test is unchanged and green, because the app is not in the API's route table.

### E014 | 06:25 | TASK DONE S02.1-T03 (API client)
- Branch `feat/S02.1-T03-api-client`, stacked on T02.
- **Installed:** openapi-fetch 0.17.0 (dependency, shipped) and openapi-typescript 7.13.0 (dev). Both are registered as installed.
- **Code:**
  - The script `pnpm generate` (`openapi-typescript ../api/openapi.yaml -o src/lib/api/schema.d.ts`) writes the types, 1,004 lines covering every path of the spec. The file is committed like the Go generated code, and excluded from Prettier and ESLint.
  - `src/lib/api/client.ts`:
    - `createApi({ baseUrl, fetch })`, whose base URL and fetch can be replaced in tests;
    - the shared `api` for this origin (`/api/v1`, from the spec's `servers`);
    - `unwrap()`, which returns the data or throws an `ApiError`.
  - `src/lib/api/errors.ts`:
    - `ApiError` (status, code, rule, detail, correlationId);
    - `fromResponse` (problem bodies, or `unexpected_response` with the `X-Request-ID`), `unreachable` (status 0), and `isProblem`.
- **Checked by running** against a live server (127.0.0.1:18092), loading the modules through Vite's module loader from a throwaway script:
  - health: "ok dev, 5 checks"; `POST /files/folders` created `/docs`; the listing showed `docs`.
  - Missing item: `ApiError 404 not_found`, with the correlation ID. Reserved name: `ApiError 400 invalid_name reserved_name`, with the correlation ID. Unreachable server: `ApiError 0 unreachable`.
- **Checks:** web format, lint, and check (0 errors, 183 files) OK. The drift check joins CI in T05.

### E015 | 06:35 | TASK DONE S02.1-T04 (design system) + BUG S02-B02
- Branch `feat/S02.1-T04-design-system`, stacked on T03.
- **Installed:** @lucide/svelte 1.48.0 (ISC, shipped; also a day old, so a second name in `pnpm-workspace.yaml`). @playwright/test 1.63.0 (dev) was installed early, for the by-hand browser checks of S02.1–S02.7, with the installed Edge and no browser download; its tests come in S02.8. Both are registered.
- **Code:**
  - `src/app.css`:
    - semantic tokens (bg, surface, surface-2, border, border-strong, fg, fg-muted, accent/hover/fg/soft, danger/hover/fg, success, warning, focus, overlay) through Tailwind 4 `@theme inline`;
    - light values on `:root` and `[data-theme=light]`; dark values on `[data-theme=dark]` and in `prefers-color-scheme: dark` unless light is chosen;
    - the page background and colors, and the focus ring.
  - `src/lib/util/theme.svelte.ts`: `Theme` (storage and root passed in), `readTheme`, `applyTheme`, and `theme()`. `+layout.svelte` applies it at startup.
  - `src/lib/components/`:
    - Button, IconButton, TextField, Checkbox;
    - Dialog (native `<dialog>` with `showModal`: focus trap, Escape, inert page, focus return; a backdrop click closes it unless `persistent`);
    - Menu (at a position, fitted into the window; arrows, Home, End, Enter; Escape, Tab, a click outside, or a resize closes it; focus returns);
    - Toast, ProgressBar (width through `style:`, which the CSP allows), Spinner, EmptyState, Breadcrumbs, Tooltip, ThemeSwitch;
    - `icon.ts` (the Lucide icon type).
  - `src/routes/dev/components/`: the component gallery, **development builds only** (production answers 404). It shows every component in the light and dark themes side by side, and live dialog, menu, and API examples.
  - ESLint: `svelte/no-navigation-without-resolve` is off, because the app is always served at the root of its origin, with no base path.
- **Checked by running:**
  - A headless Edge screenshot of the gallery (dev server) shows both themes correctly.
  - Playwright with Edge on the dev server:
    - Dialog: it opens, focus is inside, Escape closes it, and focus returns to the opener.
    - Menu: items Open, Rename, Copy, Delete with shortcuts; the first item is focused, End reaches Delete, and Enter picks it.
    - API: listing `/` answers "OK: / (0 items)"; `/nope` gives "ApiError 404 not_found (request …)".
    - Theme: Dark sets `data-theme=dark` and survives a reload (bg `rgb(11,18,32)`). Light overrides the OS dark setting. System follows the emulated OS: light `rgb(248,250,252)`, dark `rgb(11,18,32)`.
  - **Contrast** (WCAG 2.1 AA, computed for 28 pairs per theme): first run, light `warning` on `surface-2` was 4.49 < 4.5, so warning became `#9a5c06` (at least 4.91 on every light surface). Now all pass; the tightest are light warning on surface-2 at 4.91 and dark danger on surface-2 at 5.31.
  - The production build embedded in a binary: `/dev/components` shows the app's 404.
- **Bug S02-B02** (found in the production check, fixed):
  - The page logged two CSP violations: "Applying inline style violates … style-src 'self'". The cause is SvelteKit's route announcer (for screen readers), which has a static `style` attribute (visually-hidden positioning) that the strict style-src blocked.
  - Fix: `style-src 'self' 'unsafe-inline'`. Scripts stay hash-only. SvelteKit adds no style hash, so `'unsafe-inline'` takes effect.
  - After the fix: the announcer has its style, and the console shows no errors on `/` or `/dev/components`.
  - The regression test is for S02.8: no CSP violation on the main pages, and the announcer is visually hidden.
- **Checks:** web format, lint, and check (0 errors, 219 files), and build, all OK.

### E016 | 06:39 | TASK DONE S02.1-T05 (CI and project hygiene)
- Branch `feat/S02.1-T05-ci`, stacked on T04.
- **CI (`.github/workflows/ci.yml`):**
  - New job **`web`** (ubuntu): pnpm/action-setup v6.1.0 (pnpm from `packageManager`, store cached) and actions/setup-node v7.0.0 (Node.js 24.21.0).
  - Its steps: frozen install, `format:check`, `lint`, `check`, the API client drift check (`pnpm generate`, then `git status` clean under `src/lib/api`), `node ../scripts/check-web-licenses.mjs`, `pnpm audit --prod`, `pnpm build`, and upload of `web/build/` as the artifact `web-build`.
  - Job **`build`** now `needs: web` and downloads `web-build` into `web/build/` (actions/download-artifact v8.0.1) before `go build`, so the three artifacts embed the interface.
  - Dependabot: npm updates for `/web`.
- **`scripts/check-web-licenses.mjs`:**
  - Shipped packages (`--prod`: 50) are held to `allowed-licenses.txt`, and all packages (197) to that list plus the permissive tool licenses **BlueOak-1.0.0** (minimatch, linters) and **Python-2.0** (argparse, via js-yaml in openapi-typescript).
  - SPDX `OR` needs one allowed part and `AND` needs all.
  - The first run found argparse (Python-2.0), traced with `pnpm why`. The policy and reasons are in `docs/licensing.md`.
- **Audit:** `pnpm audit --prod` found one low advisory, **GHSA-pxg6-pf52-xh8x** (cookie below 0.7.0, via @sveltejs/kit). Kit uses cookie only in its server runtime, which the static build does not ship: cookie's code (`fieldContentRegExp`) is not in `web/build`. The advisory is ignored by ID in `web/pnpm-workspace.yaml` (`auditConfig.ignoreGhsas`) with that reason, and the audit now exits 0.
- **Docs:**
  - README Development section: a new "The web interface" part (build, run, `pnpm dev` at localhost:5173), two rows in the checks table, and the CI sentence.
  - `scripts/README.md` and `docs/licensing.md` updated.
- **Checked by running:**
  - A linux/amd64 binary cross-built after `pnpm build` (as the CI build job does) and run in WSL serves the app. `/files/docs` has the `_app/immutable/entry/start…` script, `/` is `200 text/html`, and the "not part of this build" warning does not appear.
  - License check and audit exit 0 locally. Web format, lint, and check are OK.
- **Next:** a deliberate lint error is pushed to see the `web` job fail, then reverted (the acceptance criterion).

### E017 | 06:46 | TASK DONE S02.2-T01 (layout and routes)
- Branch `feat/S02.2-T01-shell`, stacked on `feat/S02.1-T05-ci`. S02.1-T05's CI (deliberate lint error, revert) is being watched. S02.1 closes when T05 is merged.
- **Code:**
  - `src/routes/+layout.svelte` (the shell):
    - a skip link;
    - the header with the app name and a theme button (system, light, dark in turn; its label says the current and the next theme);
    - a side navigation (`md` and wider) or a bottom navigation (phones);
    - `<main id="main">`.
  - `src/lib/shell/sections.ts` (Files, Photos, Settings; `inSection`) and `Nav.svelte` (`aria-current="page"`).
  - `src/lib/util/paths.ts`: `segments`, `join`, `parent`, `basename`, `child`, `filesHref` (each name percent-encoded), `pathFromParam`, `crumbs`.
  - **Routes:**
    - `/` redirects to `/files` (`+page.ts`);
    - `/files/[...path]` shows the breadcrumbs; the browser comes in S02.3;
    - `/photos` is a placeholder ("Photos come with stage 4");
    - `/settings` has the theme switch and **About**: the version from the health endpoint, the license with the source repository link, and the web interface's components;
    - `+error.svelte`: not-found and error states with "Go to Files".
  - **About components:** `scripts/about.mjs` writes `src/lib/generated/components.json` (the `dependencies` of `package.json`, with installed version and license) before `dev`, `check`, and `build`. It is git-ignored and Prettier-ignored.
  - `ThemeSwitch` keeps its natural width (`self-start`).
- **Checked by running** (the built app in a binary, Playwright with Edge):
  - `/` goes to `/files`. `/files/docs/a%20b%23c` shows the title "a b#c" and the breadcrumbs "Files docs a b#c", with Files current in the navigation.
  - `/photos` and `/settings` are current in their sections. `/nowhere` shows "There is no page at this address".
  - Back, forward, and reload keep the page. The version is "dev", and the components table has 5 rows.
  - At 390 px the side bar is hidden and the bottom bar shown.
  - Screenshots (desktop light, phone dark) look right.
  - The only console entry is SvelteKit logging the 404 for `/nowhere`.
- **Checks:** web format, lint, and check (0 errors, 234 files), and the build, all OK.

### E018 | 06:52 | TASK DONE S02.2-T02 (errors and loading) + BUGS S02-B03, S02-B04
- Branch `feat/S02.2-T02-errors`, stacked on S02.2-T01.
- **Code:**
  - `src/lib/api/messages.ts`: `describe(error)` gives a title, message, and optional detail in plain words.
    - It covers every problem code of the spec and the client codes `unreachable` and `unexpected_response`.
    - `invalid_name` is explained by its `rule`, and `conflict` uses the server's detail.
    - The request ID is shown for `internal`, `unexpected_response`, and `unavailable`.
  - `client.ts`: `onApiError(listener)`; `unwrap` reports every ApiError to the listeners before throwing it.
  - `src/lib/shell/connection.svelte.ts`: `Connection` (online and checking states; the check and retry interval are passed in). `lost()` starts checking the health endpoint every 5 s, and `retry()` checks at once. `connection()` subscribes to `unreachable` errors.
  - `ConnectionBanner.svelte`: "Can't reach the NAS. … The app keeps trying." with "Try now".
  - `activity.svelte.ts`: `activity.track(promise)` and `pending`.
  - `ErrorPanel.svelte`: the described error, with "Try again".
  - **Shell:** a 2 px activity bar (while navigating or tracked work runs), the banner under the header, and a retry on the browser's `online` event.
  - **Files page:** loads the folder's details (`limit=1`; the browser comes in S02.3), shows a spinner, and on failure the error panel.
- **Bug S02-B03** (found by the check, fixed): every folder load showed "Something went wrong https://svelte.dev/e/state_unsafe_mutation". The page called `activity.track()`, which writes `$state`, inside `$derived.by`. Fix: the load starts in an `$effect`.
- **Bug S02-B04** (found by the check, fixed): with the server down, the page stayed on its error after the server came back, because of an endless effect loop (`effect_update_depth_exceeded`, about 20 folder requests in a burst). `pending++` inside `track()` reads the `$state`, so the effect depended on it and ran again on every change. Fix: `track()` updates the counter inside `untrack()`, so it is safe from any effect.
- **Checked by running** (a binary with the built app, Playwright with Edge, the server started, killed, and started again by the script):
  - `/files` shows the folder with exactly **1** request.
  - `/files/nope` shows "Not found. It may have been moved, renamed, or deleted." with Try again.
  - Server stopped, then a click on Files: the banner appears, and the panel says "Can't reach the NAS…" (1 request, no loop).
  - Server started again: the banner went away by itself (automatic retry), and "Try again" then showed the folder.
  - No page errors.
- **Checks:** web lint and check (0 errors, 240 files), and the build, OK.

### E019 | 06:57 | TASK DONE S02.2-T03 (notifications and progress) + BUG S02-B05 + S02.1 CLOSED
- **S02.1 closed:** T05's final run 36083213562 is green, 13 jobs.
  - The `web` job ran every step.
  - The three build jobs downloaded the `web-build` artifact (54,789 bytes) before `go build`, so the binaries carry the interface.
  - T05 is merged into `develop` (86dd041).
  - **Substage criteria:**
    1. Same origin, no outside requests: T02's offline render in Edge.
    2. Themes switch at runtime and follow the OS: T04.
    3. Client generated, CI fails on drift: shown locally now. A one-word change to the spec, then `pnpm generate`, made `git status` list `web/src/lib/api/schema.d.ts`, so the CI step would fail. The spec was restored.
    4. Lint and type checks in CI: the `web` job; the deliberate lint error failed it in run 36083015096.
- Branch `feat/S02.2-T03-notifications`, stacked on S02.2-T02.
- **Code:**
  - `src/lib/shell/toasts.svelte.ts`: `Toasts` (the timer is passed in; info and success leave after 5 s, errors stay until dismissed).
  - `tasks.svelte.ts`: `Tasks` and `Task` (`update`, `finish` with a success toast, `fail` with an error toast from `describe()`, `cancelled` with an info toast; an ended task leaves the list).
  - `Toaster.svelte`: a notifications region at the bottom right, above the bottom bar on phones.
  - `ProgressPanel.svelte`: "In progress (n)", foldable, one progress bar each, and Cancel labelled "Cancel <task>".
  - The shell mounts both. `Connection` gets an `onback` callback that shows "Connected to the NAS again."
  - The gallery gets demo tasks (finish, fail, cancel) and an info note.
- **Checked by running:**
  - Dev server, gallery:
    - Three demo tasks show as "In progress (3)" (bar at 20% after 0.7 s). Cancel of the third, then the other two ending, gives toasts: "Demo task (cancel): cancelled", "Demo task (fail): Not enough space. The NAS does not have enough free space for this.", and "Demo task finished." The panel is gone afterwards.
    - An info note leaves after 5 s, as do the success and cancel notes. The error stays until Dismiss.
    - The check found that all Cancel buttons had the same name, so they are now named after their task.
  - Built binary: with the server stopped, moving to Files from Settings shows the banner, and Photos still opens. With the server started again, "Connected to the NAS again." appears and the banner goes. No page errors.
- **Bug S02-B05** (found by the check, fixed): with the server down, moving to a section not yet visited (from `/settings` to Files) replaced the app with the browser's connection error page. SvelteKit could not load the route's code, fell back to a full page load, and that failed too. Fix: `data-sveltekit-preload-code="eager"` on `<body>`, so the code of every link loads as soon as the link is shown.
- **S02.2 substage criteria:** deep links, back, and forward (T01); plain error messages, never a blank screen (T02); progress and notifications (T03). All met, and S02.2 closes when T01–T03 are merged.
- **Checks:** web format, lint, and check (0 errors, 246 files), and the build, OK.

### E020 | 07:04 | TASK DONE S02.3-T01 (virtualization prototype): keep @tanstack/svelte-virtual
- Branch `feat/S02.3-T01-virtual`, stacked on S02.2-T03. S02.2-T01 is merged (CI green; `develop` c5fb225).
- **Installed:** @tanstack/svelte-virtual 3.13.39 (MIT, shipped), registered.
- **Prototype:** `src/routes/dev/virtual/` (development builds only), 50,000 synthetic items:
  - a list of 40 px rows, or a grid of 160 px tiles with the column count following the width;
  - ARIA grid rows and cells; arrow keys, Home, and End with `scrollToIndex`; an item count field.
- **Finding for the file browser:** the adapter's store starts on first subscription, before `bind:this` has set the scroll element, so an `$effect` must hand it the element and the counts through `setOptions()`. That effect first **looped** (`effect_update_depth_exceeded`), because `setOptions()` writes the store the effect read. The rule: call `setOptions()` inside `untrack()`.
- **Measured** in Edge with Playwright on the dev server, long tasks through `PerformanceObserver('longtask')`:
  - At the start, 28 rows are rendered (the scroll height is 2,000,000 px).
  - Mouse wheel, 60 steps of 900 px in 2.4 s: **no long task over 50 ms**; the first row moved to 1,343.
  - Setting scrollTop to the middle (as when dragging the scrollbar): first row 24,993, with 36 rows rendered.
  - Keyboard: End focused item 49,999 and showed row 50,000; Home returned to row 1.
  - Grid at 1,200 px wide: 5 cells a row, 10,000 rows. Wheel, 40 steps of 1,200 px: no long task over 50 ms.
  - No page errors after the fix.
- **Decision** (ADR-0009, an agent-decides item): **keep @tanstack/svelte-virtual**, with `setOptions()` untracked. The custom windowing fallback is not needed.
- **Checks:** web format, lint, and check (0 errors, 252 files) OK.
