# Session S005: 2026-09-24

| Field | Value |
|---|---|
| Session number | S005 |
| Date | 2026-09-24 |
| Start time | 06:19 +0500 (system clock at R1: 2026-09-24 06:19:45 +0500) |
| Editor | Claude Code (VS Code extension), Windows 11 |
| Model | Claude Opus 5.5 (`claude-opus-5-5`) |
| Plan version at start | 0.4.0 (Draft; on `docs/S004-video-streaming` and `main`) / 0.3.1 (on `develop` before this session's merge) |
| Active stage / task at start | none (S01 Planned) |

## Startup

- **Documents read:** RULES.md 1.3.0, CURRENT_STATE.md, plan.md 0.4.0, session log S004 (which has a closing summary), `stages/S01-basic-nas.md` (Planned), and ADR-0001-0020. All are in this agent's context from S001-S004.
- **Previous session ended cleanly?** Yes (S004 closing summary).
- **Git state (R1 step 6):**
  - The user merged the docs PRs on GitHub: #1 (`docs/P003-technology-stack`, which contains S001/P002/P003) and #2 (`docs/P004-documentation-audit`) into `develop`, and #3 (`docs/S004-video-streaming`) into **`main`**.
  - Fetched: `origin/develop` = `810727e` (plan 0.3.1); `origin/main` = `121444e` (plan 0.4.0).
  - **Missing on develop: commit `cf60f72`** (plan 0.4.0, ADR-0020, plan_v0.3.1 archive, S004 closing), because PR #3 went to `main` (the repository's default branch is `main`).
  - Unrecorded commit found: **`bda8321 "added more prompts"`** by the user (06:19) on the local `docs/S004-video-streaming` branch. It adds `prompts/3-…` and `prompts/4-…`, which resolves A001 D-10. The user confirmed it is theirs (E004).
- **Resume summary given to the user:**
  > No active stage; S01 Planned; plan 0.4.0 draft. Last completed: A001 + video change. Next: the approval stage (D-01-D-14, then plan 1.0.0 and S01 approval).

## Entries

### E001 | 06:19 | USER
"All branches merged into develop, so now take pull from develop, see if it is updated and not anything missing then get to working"
(followed by "Continue from where you left off.")

### E002 | 06:20 | ACTION
- `git fetch --prune`, then compared `origin/develop` and `origin/main`. The only content difference was `cf60f72` (on main, not on develop).
- `git switch develop` and `git pull --ff-only` brought local develop from `c34cc21` to `810727e`.
- Reported to the user that develop lacked plan 0.4.0 / ADR-0020.

### E003 | 06:20 | USER
"stop, bring the last commit in docs/s004... to develop, i did that mistakenly there"

### E004 | 06:21 | ACTION + USER
- `git merge --no-ff docs/S004-video-streaming` into develop created merge commit `c535cda`, which brings in `cf60f72` (plan 0.4.0) and the user's `bda8321` (prompts 3 and 4). No conflicts. Pushed: `origin/develop` `810727e..c535cda`.
- This is a merge directly onto develop, done at the user's **explicit instruction** (E003), which overrides the "never commit directly to develop" preference for this instance (R10).
- The user then said: "that commit is mine" (about `bda8321`).

### E005 | 06:22 | ACTION (verification)
- All five docs branches are ancestors of develop. The develop content equals main plus `prompts/3` and `prompts/4`.
- Checks: plancheck (179 requirements, 93 substages, no problems); all tables consistent; audit script 102 pass.
- Two script hits, neither a real problem:
  - (a) bootstrap prompt vs `prompts/1` not byte-identical in the working copy. Same git blob `acfb0be`; only the checkout line endings differ (`core.autocrlf=true`; `.gitattributes` is planned in S01.1-T01).
  - (b) ADR-0020's approval cites S004 rather than P003 (the script's rule is too narrow).
- **Develop is complete.**
- Created branch `docs/S005-approval-baseline` for this session's approval-stage documentation. `main` does not have `bda8321` yet; it will get it through a normal develop → main merge later.
- R10 question for the user (asked in the report): should the develop-merge exception above become a lasting preference? Not assumed.

### E006 | 06:25 | AGENT → USER
- Resume summary.
- Suggested making `develop` the GitHub default branch (user action).
- Presented the decisions table (D-01-D-07, D-09, D-11, D-14 with recommendations) and asked four questions (approvals; D-12 license; D-08 first release; D-13 hardware and library size).

### E007 | 06:25 | USER
Answers (options selected, verbatim):
1. Approvals: "Accept all (Recommended)" (D-01-D-07, D-09, D-11, D-14 as recommended in the table).
2. D-12 license: "AGPL-3.0 (Recommended)".
3. D-08 first release: "S01–S11, add none".
4. D-13 hardware (free text): "mini pc/old pc/raspberry pi/also this windows 11 pc (this one for testing) so multi platform compatibility". **No library size given** (Q18 stays open).

### E008 | 06:25 | USER (mid-turn message)
"when you are done with a branch, you yourself should do a merge of that branch into develop (another rule you should remember)"

### E009 | 06:25 | DECISION
- A lasting preference under R10: recorded in RULES.md User Preferences without asking. It supersedes the earlier "never merge PRs" part of the preference for merges **into develop**.
- **Decisions to apply in S005:**
  - D-01: ADR-0003 → Accepted.
  - D-02: the 10.14 flags are approved.
  - D-03: stacked docs branches allowed; the new merge rule applies.
  - D-04: Debian's GPL FFmpeg accepted for now (revisit S11.1).
  - D-05: README R-01-R-08 and R-10 applied (R-09 not approved; it was optional).
  - D-06: semantic search precomputed-only by default; a query-time model needs separate approval.
  - D-07: stderr + a rotated JSON log file from S01.
  - D-08: first usable release = S01-S11; Q37 none.
  - D-09: targeted plan reading (R1 change).
  - D-11: Q16 closed.
  - D-12: AGPL-3.0 (Q22 answered).
  - D-13: Q1 answered (x86-64 mini-PC/old PC + Raspberry Pi ARM64 + Windows 11 for testing; multi-platform).
  - D-14: the ADR-0020 side effect is confirmed.
- **Not yet given:** explicit approval of the plan as the **1.0.0 baseline** and of the **S01 stage document**. These will be asked after the decisions are applied (R3/R4).

### E010 | 06:29 | ACTION (decisions applied)
- **RULES.md 1.3.0 → 1.4.0:**
  - R1 step 3 and the Quick start: targeted plan reading (D-09).
  - User Preferences: the merge-into-develop rule (E008) and stacked branches (D-03); the S001 branching bullet is marked partly superseded.
  - Two changelog rows.
- **plan.md 0.4.0 → 0.5.0** (archived `plan_v0.4.0.md` first; 34 unique replacements):
  - Q1, Q16, Q22, Q37, Q38 answered; ★ legend and baseline list updated.
  - A13, A19; NFR-003, NFR-009, NFR-016; FR-054; S12.10 (D-06); S04.8 (D-14).
  - Section 7 ADR-0003 Accepted; 7.1 updated; 6.3 logs (D-07).
  - 10.14 approved (D-02); section 11 M3 chosen (D-08).
  - RK-08, RK-30; revision entry 0.5.0.
- **ADR-0003:** Proposed → **Accepted** (approval record quotes E007). **ADR-0020:** D-14 confirmation appended to the approval record.
- **README.md:** R-01-R-08 and R-10 applied (D-05); R-09 not applied.
- **stages/S01-basic-nas.md:** gates removed; T02 set to AGPL-3.0; T08 and 4.2 add the rotated log file; T04 reference hardware; based on plan 0.5.0; change-log row. Status stays **Planned**.
- **dependencies.md:** project license AGPL-3.0; FFmpeg D-04 decision.
- **Audit A001:** new section 9 "Decisions received (S005)". README proposal status set to Decided/applied.
- **CURRENT_STATE** rewritten (the approval stage is waiting for explicit approvals).
- **Checks:** audit script (the two known narrow hits only); plancheck (no problems); all tables consistent, including README.md.
- NOTE: one side-document script failed twice on a pattern (the S01.7-T04 text sits in the acceptance column). No partial writes happened; the script was fixed and re-run cleanly.

### E011 | 06:31 | AGENT → USER
Asked three questions: approve plan 0.5.0 as the 1.0.0 baseline; approve the S01 stage document; how to get Go (not installed; winget and Docker available).

### E012 | 06:31 | USER
Answers (options selected, verbatim):
1. "Approve as 1.0.0 (Recommended)"
2. "Approve S01 (Recommended)"
3. "Install via winget (Recommended)"

### E013 | 06:31 | DECISION
- **Plan 1.0.0 baseline approved (R4)** and **S01 stage document approved (R3)**. Implementation of S01 may begin.
- Permission given to install Go via winget.

### E014 | 06:31 | ACTION (baseline)
- R4: plan 0.5.0 archived (`plan_v0.5.0.md`, byte-identical), then plan → **1.0.0**, status "Approved baseline", S01 status Approved (overview table and section 10.2), revision row 1.0.0 quoting E012.
- R3: `stages/S01-basic-nas.md` status → **Approved**, approval record quotes E012, based on plan 1.0.0, change-log row.
- CURRENT_STATE: phase = implementation; active stage S01; write-ahead for merge + Go install + S01.1-T01.
- Checks: plancheck no problems; tables consistent.

### E015 | 06:36 | USER (message during the Go install)
> one thing to add: keep record of all dependencies needed, in the end you will have to make a setup script, a separate one for each platform, which when run, will automatically handle the deployment.

- R4 impact analysis: a new requirement, so plan MINOR → **1.1.0**. It affects S11.2 (native installation becomes per-platform setup scripts), Q5 (partly answered: Windows 11 gets a script; macOS still open), concern 8.20, and `dependencies.md` (a per-platform prerequisite record is needed from now on). No written code is affected (none exists yet). S01 content is unchanged: S01 needs nothing on the target beyond the binary.
- The intent is clear, so I applied it without a confirmation round (R4 step 3). One real choice remains, so I recorded it as **Q41** (Linux script default: Docker Compose or native service; needed by S11.2) with a recommendation.
- Lasting instruction → RULES User Preferences (R10).

### E016 | 06:36 | ACTION (plan 1.1.0) on `docs/S005-setup-scripts`
- Plan 1.0.0 archived (`plan_v1.0.0.md`); plan → **1.1.0**: FR-149 (setup script per platform), NFR-032 (dependency record, per-platform runtime prerequisites), S11.2 rewritten, S11.4 scope, Q5 updated, Q41 added, 8.20 bullet, revision row quoting E015.
- `dependencies.md`: new **section 12** (deployment prerequisites per platform: Linux x86-64, Raspberry Pi, Windows 11, Docker image), with verification notes per row.
- RULES 1.4.0 → **1.5.0**: User Preferences bullet (dependency record + per-platform setup scripts) and changelog row.
- S01 document: based on plan 1.1.0; section 7 note (no runtime prerequisite in S01); acceptance checklist includes section 12; change-log row.
- CURRENT_STATE updated (plan 1.1.0, pointers).

### E017 | 06:36 | ACTION (Go installed)
- `winget install --id GoLang.Go --exact` (user-approved, E012) finished with exit code 0: **go version go1.27.0 windows/amd64** at `C:\Program Files\Go`, `GOTOOLCHAIN=auto`. winget has 1.27.0, not 1.27.1. The pinned go1.27.1 comes in through the `toolchain go1.27.1` line in `go.mod` (downloaded automatically from the Go module proxy on the first go command, a build-time action only).
- A new shell needs the updated PATH. Until then, commands call `C:\Program Files\Go\bin\go.exe` directly.
- Checks for plan 1.1.0: plancheck no problems (NFR max 32, questions up to 41); all tables consistent; `plan_v1.0.0.md` matches the committed 1.0.0 apart from line endings.

### E018 | 06:37 | TASK START S01.1-T01 (write-ahead)
- `develop` = c85a120 (includes plan 1.1.0). Branch `feat/S01.1-T01-go-module` created from it.
- Statuses: S01 **In Progress** (stage doc, plan 10.1 and 10.2), S01.1 In Progress, S01.1-T01 In Progress; CURRENT_STATE write-ahead.
- Environment: Go 1.27.0 local, pinned toolchain go1.27.1 through `go.mod`. The Docker CLI is present but the daemon is not running (Docker Desktop not started). Linux is therefore checked by cross-compiling (`GOOS=linux` build + vet) here, and CI (S01.1-T05) runs the real Linux job.
- OpenAPI stub uses **3.0.3**: oapi-codegen v2 targets OpenAPI 3.0.x (ADR-0002 says "OpenAPI 3"). S01.5-T05 confirms it with the full spec.

### E019 | 06:41 | TASK DONE S01.1-T01 (Go module and repository skeleton)
- Created: `go.mod`/`go.sum` (`module github.com/KhizirFarrukh/local-ai-nas`, `go 1.27`, `toolchain go1.27.1`, `tool` directives: oapi-codegen v2.8.0, govulncheck v1.8.0, go-licenses v2.0.1); `cmd/local-ai-nas/main.go` (prints the version; subcommands in T11) + `main_test.go`; `internal/{config,logging,apperr,db,health,api}/doc.go`; `api/openapi.yaml` (3.0.3 stub, no paths); `deploy/README.md`, `docs/README.md`, `scripts/README.md`, `testdata/SOURCES.md`; `.gitattributes` (`* text=auto eol=lf`; CRLF for .bat/.cmd/.ps1; binary patterns), `.editorconfig`, `.gitignore`.
- Checks (go1.27.1 windows/amd64, downloaded automatically through the toolchain line):
  - `go build ./...`, `go vet ./...` OK; `go test ./...`: `cmd/local-ai-nas` ok (TestRunPrintsVersion), other packages have no test files yet.
  - Linux: `CGO_ENABLED=0 GOOS=linux GOARCH=amd64|arm64 go build ./... && go vet ./...` OK (cross-compile; the real Linux job comes with CI in T05).
  - `gofmt -l` clean. `go tool oapi-codegen -version` = v2.8.0; `go tool govulncheck -version` = govulncheck@v1.8.0; `go tool go-licenses --help` runs.
  - Renormalize: `git write-tree` before and after `git add --renormalize .` are identical (e9c4549…); every file in the index is LF.
- R6: `dependencies.md` updated (Go pin note; the three tools marked "in go.mod"; note on how Go records tool modules as indirect requires). Direct requirements in `go.mod` = the three tools, matching section 3. No new runtime prerequisite (section 12 unchanged).
- Not done in T01 (planned elsewhere): README "Development" section (T06), LICENSE (T02), lint config (T03).

### E020 | 06:43 | USER DECISION (license version form) + TASK START S01.1-T02
- T01 merged: `develop` = 0100920 (pushed).
- The records said only "AGPL-3.0", so I asked whether the license is "version 3 only" or "version 3 or any later version". User's answer: **"AGPL-3.0-or-later (Recommended)"**. The SPDX identifier is `AGPL-3.0-or-later`.
- Branch `feat/S01.1-T02-license` from `develop`; T02 In Progress (stage doc, CURRENT_STATE write-ahead).

### E021 | 06:47 | TASK DONE S01.1-T02 (license and license policy)
- `LICENSE`: the GNU AGPL v3 text. gnu.org could not be reached from this machine (3 curl attempts, connect timeout on port 443, IPv4 and default). Source used: GitHub's license API (`api.github.com/licenses/agpl-3.0`, field `body`): 661 lines, 34,523 bytes (the size of FSF's `agpl-3.0.txt`), sha256 `8486a10c4393cee1c25392769ddd3b2d6c242d6ec7928e1414efff7dfb2f07ef`. Cross-checked against the SPDX license list (`text/AGPL-3.0-or-later.txt`): all 5,535 words match; the only 3 differences are `http` vs `https` in URLs (FSF's current file uses https).
- README "License" section: **AGPL-3.0-or-later**, a one-line summary of the network clause, and a link to the policy.
- `docs/licensing.md`: the project license; the allowed licenses for linked or bundled code with reasons (MIT, BSD-2/3, ISC, Apache-2.0, MPL-2.0, LGPL-2.1/3.0, GPL-3.0, AGPL-3.0); what is not allowed (GPL-2.0, including -or-later without manual review; NC/research-only; SSPL/BUSL; unknown; public-domain dedications not listed yet); external programs; dev tools; datasets and models; how it is checked; steps for adding a dependency (links R6 and section 12).
- `scripts/allowed-licenses.txt` (the allow-list) and `scripts/check-licenses.sh` (`go tool go-licenses check ./... --allowed_licenses=<list>`; executable bit set in git).
- Checks:
  - `scripts/check-licenses.sh` exit 0 (the only non-stdlib module is the project itself, detected as AGPL-3.0).
  - **Negative check:** a temporary local module `example.com/badmod` (Unlicense text; `replace` in the scratchpad) imported from `main.go` gives "Not allowed license 'Unlicense' found for library 'example.com/badmod'.", exit 1. It was then reverted with `git checkout -- go.mod cmd/local-ai-nas/main.go`, and the check passes again.
  - `go build`, `go vet`, `go test ./...` OK.
- Plan 1.1.0 → **1.1.1** (PATCH, R4): Q22 and RK-08 record "or later"; 1.1.0 archived. `dependencies.md` license line and go-licenses row updated. S01 document: T02 Done, section 6 row, change-log row. CURRENT_STATE updated.

### E022 | 06:47 | TASK START S01.1-T03 (write-ahead)
- T02 merged: `develop` = b0cc2ec (pushed). Branch `feat/S01.1-T03-lint` from `develop`; T03 In Progress.
- Install approach: golangci-lint v2.13.2 as a pinned binary in `./bin` (git-ignored) through the upstream `install.sh` taken from the **v2.13.2 tag**, which verifies the release checksum. Wrapped in `scripts/install-golangci-lint.sh`.

### E023 | 06:48 | TASK DONE S01.1-T03 (lint and format)
- `scripts/install-golangci-lint.sh`: runs the upstream `install.sh` from the v2.13.2 tag (it verifies the checksum) into `./bin` (git-ignored). Result: `golangci-lint has version 2.13.2 built with go1.27.0 from 27774aaf on 2026-08-27T23:01:12Z` (windows/amd64).
- `.golangci.yml` (v2): `linters.default: none` + depguard, errcheck, gosec, govet, ineffassign, staticcheck, unused. depguard rules `files-area` (internal/files must not import internal/photos) and `photos-area` (the reverse), with I1 messages. Formatters gofmt + goimports (local prefix = module path). Unlimited issue counts.
- Checks: `golangci-lint config verify` exit 0; `golangci-lint run ./...` "0 issues."; `golangci-lint fmt --diff` exit 0.
- **Deliberate violations** (temporary fixtures `internal/files/f.go` → `internal/photos`, `internal/lintfixture/l.go` with an unchecked `os.Remove` and bad formatting): run exit 1 with 3 issues (depguard "not allowed from list 'files-area'", errcheck, gofmt); `fmt --diff` exit 1. The fixtures were removed and the run is clean again. depguard's file globs work with Windows paths.
- Limitation: depguard cannot express "only internal/transfer may import both areas". It is recorded in the S01 change log as an architecture test for S04.6 (no package imports internal/photos before S04).
- The CI half of the acceptance ("fails in CI") is verified in T05.
- `dependencies.md`: golangci-lint row names the install script.

### E024 | 06:50 | TASK START S01.1-T04 (write-ahead)
- T03 merged: `develop` = f0cc48c (pushed). Branch `feat/S01.1-T04-testing`; T04 In Progress.
- Design: `internal/testutil` gets StorageRoot(t) (temp dir with symlinks resolved), NewServer(t, h) (httptest server on an explicit 127.0.0.1:0 listener), and WriteFiles/ReadFiles (fixture trees; writes through `os.Root`). Sample fuzz target: WriteFiles never creates anything outside its root, and whatever it writes reads back unchanged. The meaningful fuzz targets (resolver, names) come in S01.6. Coverage gate: `scripts/coverage.sh`. Conventions: `docs/testing.md`.

### E025 | 06:53 | TASK DONE S01.1-T04 (test setup)
- `go.mod`: `github.com/google/go-cmp v0.7.0` direct (it was v0.6.0 indirect through the tools; `go get` upgraded it).
- `internal/testutil/testutil.go`: `StorageRoot(t)` (t.TempDir + EvalSymlinks), `NewServer(t, h)` (httptest on an explicit `127.0.0.1:0` listener, closed on cleanup), `WriteFiles`/`ReadFiles` (fixture trees through `os.Root`; `ReadFiles` walks `root.FS()`), `closeWith` (keeps a Close error).
- Tests: `TestStorageRoot`, `TestNewServer` (host is 127.0.0.1; round trip), `TestWriteReadFiles` (integration on a temp root, cmp.Diff, spaces and non-ASCII names), `TestWriteFilesRejectsEscapes` (`../`, `a/../../`, `/abs`; on Windows also `C:\abs.txt`, `..\outside.txt`, UNC; asserts nothing appears next to the root), `FuzzWriteFiles` (8 seeds: never writes outside the root; successful writes read back unchanged).
- `scripts/coverage.sh`: `go test -covermode=atomic -coverprofile=coverage.out ./internal/...` with a gate (`COVERAGE_MIN`, default 80). `docs/testing.md`: commands, conventions, helpers, fuzzing, coverage, test data.
- Lint review round 1 found 7 issues. Fixes: errcheck (3): deferred Close handled (`closeWith` in the helpers, `_ =` in tests), with no blanket exclusion. gosec G304: `ReadFiles` reads through `os.Root`. gosec G301/G705 in `_test.go` (3): `.golangci.yml` now excludes gosec **for test files only** (tests build hostile inputs on purpose). Round 2: 0 issues.
- Results (Windows, go1.27.1): `go vet` OK; `go test ./...` OK; coverage **83.0%** (gate passes; with `COVERAGE_MIN=99` it fails: "FAIL: coverage 83.0% is below 99%", exit 1); `go test -fuzz ^FuzzWriteFiles$ -fuzztime 45s`: 40,670 execs, 115 corpus entries (cache only, not committed), PASS; golangci-lint run/fmt clean.
- Race detector: not available on this Windows PC (it needs cgo); CI runs `-race` on Linux (T05).
- Tooling note: in this environment backslashes inside bash heredocs are halved (this caused the `\b` path bug in E019). Content with backslashes is now written through files or the Write/Edit tools.

### E026 | 06:55 | TASK START S01.1-T05 (write-ahead)
- T04 merged: `develop` = dc9c0a0 (pushed). Branch `feat/S01.1-T05-ci`; T05 In Progress.
- `gh` is not installed. The repository is **public**, so run results are read anonymously from the GitHub REST API (`/actions/runs`, `/jobs`).
- Action versions checked on the GitHub API: actions/checkout v7.0.1 (2026-07-20), actions/setup-go v7.0.0 (2026-07-16), golangci/golangci-lint-action v9.3.0 (2026-06-29; has `install-only`), docker/setup-buildx-action v4.4.1, aquasecurity/trivy-action v0.36.0 all exist. Licenses: checkout, setup-go, golangci-lint-action **MIT**. **New:** actions/upload-artifact **v7.0.1** (latest, 2026-04-10), MIT, for the cross-build artifacts (register entry in this task, R6).
- Order change (recorded in the S01 change log): T06 runs after T11 (it needs a runnable server). The image build + Trivy job and Dependabot's docker ecosystem move into T06. CI triggers on pushes too, because branches are merged by the agent without a PR.
- Also: `scripts/coverage.sh` gets its missing executable bit (it was committed as 100644 in T04).

### E027 | 07:01 | TASK DONE S01.1-T05 (CI pipeline)
- Files: `.github/workflows/ci.yml` and `.github/dependabot.yml` (gomod + github-actions, weekly, target `develop`); `scripts/coverage.sh` publishes the figure to `$GITHUB_STEP_SUMMARY` and got its executable bit.
- Workflow: triggers on pushes to `develop`, `main`, `feat/**`, `fix/**`, `docs/**`, `chore/**` and on PRs into `develop`/`main`; `permissions: contents: read`; concurrency cancels superseded runs. Jobs:
  - **Lint and format**: golangci-lint-action v9.3.0 with v2.13.2, then `golangci-lint fmt --diff`.
  - **Test** (ubuntu with `go test -race`; windows with `go test`): `go vet` first, then the coverage gate on Linux.
  - **Checks**: govulncheck, `scripts/check-licenses.sh`, `go generate` drift.
  - **Build**: CGO-free cross-builds for linux/amd64, linux/arm64, windows/amd64 (`-trimpath`, version = short SHA) uploaded as 7-day artifacts.
- Results on GitHub (read anonymously through the public REST API):
  - Commit 57f20d0: run **35945085750**, all 7 jobs **success**. Artifacts: local-ai-nas-linux-arm64 (1,336,081 B), -windows-amd64 (1,506,575 B), -linux-amd64 (1,440,863 B).
  - **Red check:** commit 6d9e690 added `TestDeliberateFailure`. Run **35945229679**: Test (windows-latest) failure at step "go test"; Test (ubuntu-latest) failure at step "go test (race detector)"; run conclusion **failure**; the other jobs succeeded.
  - Commit 61f51db reverts it (a new revert commit, no history rewrite). Run **35945387694**: all 7 jobs **success**.
- This also covers the CI halves of T03 (a lint violation fails CI: the same golangci-lint config runs in the lint job) and T04 (the coverage figure is published in the job summary; the gate step passed).
- Not verified: the job-summary text itself (reading it needs an authenticated API call; the step passed).
- Deferred to T06 (recorded in E026 and the S01 change log): the dev image build + Trivy job, the Dependabot docker ecosystem, and the setup-buildx and trivy actions.
- Meanwhile (scratchpad only, not in the repo): a T07 draft. A go-toml v2.4.3 probe showed that `time.Duration` cannot be decoded from a TOML string, and that integer values for a TextUnmarshaler type reach UnmarshalText with no key in the error. So the draft decodes the file into a map and checks every value against a settings table, so each error names its key.

### E028 | 07:02 | TASK START S01.1-T07 (write-ahead)
- T05 merged: `develop` = f662664 (pushed; its CI run is being watched). Branch `feat/S01.1-T07-config`; T07 In Progress.
- Design: one settings table. Each key (e.g. `storage.root`) maps to the file (`[storage] root`), the environment (`LOCALAINAS_STORAGE_ROOT`), and the flag (`--storage-root`). Precedence: defaults < file < env < flags. The file is decoded into a map, and every value is type-checked against the setting's kind (string, size, duration, int). Unknown keys are rejected (strict). Errors name the key and its source; all problems are reported together.
- Keys in T07 (their consumers come later): `storage.root` (required, absolute), `storage.free_space_reserve` (1GiB; S01.2-T04), `server.bind` (127.0.0.1:8080; syntax only here, loopback guard in S01.6-T06), `server.read_header_timeout` (10s), `server.idle_timeout` (2m), `server.shutdown_timeout` (30s) (T11), `log.level` (info), `log.file_max_size` (10MiB), `log.file_max_files` (5) (T08), `uploads.max_file_size` (100GiB), `uploads.max_chunk_size` (64MiB) (S01.4-T05). The defaults are the agent's choice, recorded here; the user can change them in the config file.
- Config file location: `--config`, then `LOCALAINAS_CONFIG`, then the OS default (`%ProgramData%\local-ai-nas\config.toml` on Windows, `/etc/local-ai-nas/config.toml` elsewhere). A missing default file is fine; a missing explicit file is an error.
- Secrets: S01 has no secret settings. Loaded config values are not logged in T07. S03 adds secrets and must mark them for redaction.

### E029 | 07:04 | TASK DONE S01.1-T07 (configuration)
- `internal/config`: `config.go` (Config, Default, settings table with kinds, EnvName/FlagName, Sources, DefaultConfigFile, Load, readFile via map + fromTOML type checks, Error, validate), `units.go` (ByteSize with KiB/MiB/GiB/TiB and KB/MB/GB/TB; Duration as a struct, so a bare integer is rejected), `flags.go` (RegisterFlags, SourcesFromFlags: only flags actually given count). `internal/config/doc.go` removed (the package doc is in config.go).
- `go.mod`: `github.com/pelletier/go-toml/v2 v2.4.3` (direct, linked). `go-licenses report`: MIT; `scripts/check-licenses.sh` exit 0.
- Tests (table-driven): defaults; precedence file < env < flags with origins; config file location (flag > env > default; a missing default is fine; a missing explicit file fails); file errors (unknown key or section, wrong types: boolean, integer, string, float, array, table, date; integer duration rejected; bad size/duration; not TOML with row:col; several problems in one message); every validation rule (18 cases, each asserting the key and the message, plus env and flag origins); exact message format; normalization (clean root, lower-case level); names; flags round trip; ByteSize parse (20 cases, including overflow) and String round trip; Duration.
- Results: `go test ./internal/config` OK; coverage of internal/... **95.4%**; golangci-lint 0 issues, fmt clean; `GOOS=linux GOARCH=arm64 go vet` OK.
- Acceptance: "An invalid config exits non-zero with a clear message naming the key": the message part is covered here (e.g. `config: log.level (from flag --log-level): must be debug, info, warn, or error, got "loud"`). The non-zero exit is wired with the CLI in T11.

### E030 | 07:07 | TASK START S01.1-T08 (write-ahead)
- T07: branch CI green (commit 687a2ab), merged: `develop` = 69a0f7b (pushed). Branch `feat/S01.1-T08-logging`; T08 In Progress.
- Design (drafted in the scratchpad while T07's CI ran):
  - `RotatingFile`: appends to `nas.log`. When a write would pass the max size, it renames `nas.log` to `nas.log.1` and shifts the older files up; files beyond the max count are deleted. The current file is closed before the renames, which Windows needs. If rotation fails, it keeps writing to the current file. It is safe for concurrent use.
  - `New(Options)`: slog JSON handler to stderr and the file; a tee writer, so one failing sink does not stop the other; level from config.
  - `RequestID` middleware: keeps a valid incoming `X-Request-ID` (1–128 chars of `[A-Za-z0-9._:-]`), otherwise 16 random bytes as hex; sets the response header and the context.
  - `AccessLog` middleware: one `request` line with method, route (`r.Pattern`), status, bytes, duration_ms, request_id, remote, and the query with sensitive values redacted. At debug level it also logs request headers, with credential headers redacted.
  - The logging package does not import config; T11 maps config to `logging.Options`.

### E031 | 07:08 | TASK DONE S01.1-T08 (logging)
- `internal/logging`: `rotate.go` (RotatingFile, DefaultPath, `nas.log` + `nas.log.1..N`), `logging.go` (Options, ParseLevel, New; tee writer to stderr + file), `middleware.go` (RequestID, ValidRequestID, RequestIDFrom, AccessLog, RedactHeaders, RedactQuery, recorder with Unwrap/Flush). `internal/logging/doc.go` removed.
- Tests:
  - Rotation: shifting and max-count deletion; appends to an existing file and rotates on the first write that does not fit; oversized writes; 8 goroutines × 100 lines (no line lost, no file over the limit); **rotation failure** (a non-empty directory blocks it) keeps logging into the current file; closed-file behavior; invalid settings.
  - New: identical lines on stderr and in the file; level filtering; no-file mode; errors (unknown level, a logs dir under a file, zero rotation settings); the tee writer keeps the file when stderr fails.
  - Access log fields (method, route pattern, status 418, bytes 5, duration_ms, request_id = response header, query); an unmatched route logs status 404 with an empty route, and an empty handler logs 200 and 0 bytes; request IDs (a UUID and `client.req:42_x` are kept; spaces, U+2028, 129 characters, and empty are replaced by a new 32-hex ID).
  - **Redaction:** at debug level, Authorization, Cookie, and X-Api-Key are logged as `[REDACTED]`, while Accept stays; the query keeps `name=ok` and redacts token, api_key, and Password. The secret string appears nowhere in the output.
  - Flush and SetWriteDeadline through `http.ResponseController` (Unwrap).
- Lint round 1: 1 issue (staticcheck ST1023, `var out io.Writer = stderr`), fixed. Unused `RotatingFile.Path` removed. Round 2: 0 issues.
- A heredoc again turned `\n` into real newlines in a Python-generated test; I caught it at compile time and fixed it with the Edit tool. Rule for myself: code or text with backslashes goes through Write/Edit or script files only.
- Results: `go test ./internal/logging` OK (also `-count=3`); coverage of internal/... **94.8%** (the uncovered lines are close/stat error paths); `GOOS=linux go vet` OK.

### E032 | 07:11 | TASK START S01.1-T09 (write-ahead)
- T08: branch CI green (commit c7974e0, including `-race` on Linux), merged into `develop` and pushed. Branch `feat/S01.1-T09-errors`; T09 In Progress.
- Design (drafted while T08's CI ran):
  - `Kind` with a stable code and status: internal 500, invalid_request 400, invalid_name 400, outside_root 400, not_found 404, conflict 409, too_large 413, insufficient_storage 507, not_available 501 (the reserved photos API in S01: 501 Not Implemented).
  - `Error{Kind, Detail, Err}`: Detail is client-safe, Err is the logged-only cause. `KindOf` walks the chain; anything without an `*Error` is internal.
  - Problem body (RFC 9457): `type` = "about:blank", so the title is the HTTP status text (RFC 9457 section 4.2.1); extensions `code` and `correlation_id` (= request ID). Internal errors always get a generic detail. S01.5-T03 (error catalogue) may introduce specific type URIs.
  - `Write` logs 5xx at error level with the full error and 4xx at debug level. `Recover` middleware returns a generic 500 and logs the panic value and stack; `http.ErrAbortHandler` is re-raised. Middleware order: RequestID → AccessLog → Recover → mux.

### E033 | 07:11 | TASK DONE S01.1-T09 (error conventions)
- `internal/apperr/apperr.go`: Kind (9 kinds, code + status), Error/New/Newf/Wrap/KindOf, Problem/ProblemFor (RFC 9457, `about:blank`, `code`, `correlation_id`), Write (logs 5xx at error level and 4xx at debug), Recover middleware. `internal/apperr/doc.go` removed. apperr imports logging (for the request ID); logging does not import apperr.
- Tests: every kind's code and status, plus an unknown kind that falls back to internal and a check that each code is used once; error chains (KindOf through `fmt.Errorf %w`, errors.Is reaches the cause, messages); ProblemFor for domain, wrapped, plain, and internal errors (the internal detail and the cause's paths are never in the body); Write over a real server (status 413, `application/problem+json`, `Cache-Control: no-store`, a stale Content-Length removed, correlation_id = X-Request-ID, the cause in the log); an internal error logged at ERROR with the correlation ID; a nil error gives 500; a **panic** gives a generic 500 body, with the panic value and a goroutine stack only in the log and the access log recording status 500; pass-through; `http.ErrAbortHandler` is re-raised.
- Results: tests OK on the first run; `apperr` coverage 100%, internal/... 95.4%; golangci-lint 0 issues.

### E034 | 07:14 | TASK START S01.1-T10 (write-ahead)
- T09: branch CI green (commit 4232afc), merged into `develop` and pushed. Branch `feat/S01.1-T10-sqlite`; T10 In Progress.
- Dependency probe (scratch module, not the repo): `modernc.org/sqlite v1.59.0` + `github.com/pressly/goose/v3 v3.28.0` link these modules: modernc.org/libc v1.75.7, modernc.org/mathutil v1.7.1, modernc.org/memory v1.12.1, github.com/dustin/go-humanize v1.0.1, github.com/google/uuid v1.6.0, github.com/mattn/go-isatty v0.0.24, github.com/ncruces/go-strftime v1.0.0, github.com/remyoudompheng/bigfft, github.com/mfridman/interpolate v0.0.2, github.com/sethvargo/go-retry v0.4.0, go.uber.org/multierr v1.11.0, golang.org/x/sync, golang.org/x/sys. A binary with both is about 10 MB (windows/amd64). Licenses are checked by go-licenses in the task.
- Design: `DB{Write (1 connection, `_txlock=immediate`), Read (4 connections, `query_only`)}`; pragmas on every connection through the DSN (`journal_mode(WAL)`, `foreign_keys(1)`, `busy_timeout(5000)`, `synchronous(NORMAL)`); Open creates the directory and checks that WAL is active; goose Provider (`DialectSQLite3`) over the embedded `migrations/`; `Migrate` (up) and `MigrationStatus` for the T11 `migrate up|status` subcommand.
- Schema `00001_init.sql`: STRICT tables `settings(key, value, updated_at)` and `uploads(id, namespace, target_path, on_conflict CHECK, declared_size CHECK, sha256 CHECK len 64, created_at, expires_at)` + index on `expires_at`; times are Unix ms. **Added beyond the task text:** an `uploads.namespace` column (owner namespace, ADR-0003), so the upload index follows the owner model from the start.

### E035 | 07:16 | TASK DONE S01.1-T10 (SQLite database)
- `internal/db/db.go` (Open, dsn, DB.Write/Read, Close, Path, Migrate, MigrationStatus), `internal/db/migrations/00001_init.sql`, `internal/db/db_test.go`. `internal/db/doc.go` removed.
- `go.mod`: `modernc.org/sqlite v1.59.0` and `github.com/pressly/goose/v3 v3.28.0` (direct). `go mod tidy` also fetched modernc build-tool modules (ccgo, cc, gc) that are required only by the module graph; they are not linked.
- Tests (Windows, go1.27.1), all green on the first run:
  - The pragmas on both pools: journal_mode = wal, foreign_keys = 1, busy_timeout = 5000, synchronous = 1 (NORMAL); query_only = 1 on readers and 0 on the writer.
  - **A read succeeds during an open write transaction** and sees the committed state (1 row), then 2 rows after the commit.
  - The reader pool cannot write.
  - **Migrations are idempotent:** the first run applies version 1, the second applies nothing; status goes pending → applied.
  - The schema constraints hold: an invalid `on_conflict` is rejected, and the STRICT table rejects text in an INTEGER column.
  - Reopening keeps the data and has nothing to migrate.
  - Open fails under a regular file and on a non-database file.
- Checks:
  - `scripts/check-licenses.sh` exit 0. `go-licenses report` for the linked modules: AGPL-3.0 (this project); MIT (go-humanize, go-isatty, interpolate, go-strftime, go-toml, goose, multierr); BSD-3-Clause (bigfft, x/sync, x/sys, modernc libc/mathutil/memory/sqlite); Apache-2.0 (go-retry).
  - govulncheck: "No vulnerabilities found."
  - golangci-lint 0 issues; coverage of internal/... 93.6%; CGO-free builds for linux/amd64, linux/arm64, windows/amd64 OK.
- The Linux half of the driver test runs in CI. Section 12 of the register is unchanged: SQLite is compiled into the binary, so there is no new prerequisite on the target.

### E036 | 07:18 | TASK START S01.1-T11 (write-ahead)
- T10: the CI watcher hit GitHub's **anonymous API rate limit** (60/hour; 403 at about 07:17, reset at 07:54). I switched to the workflow **status badge** (`/actions/workflows/ci.yml/badge.svg?branch=...`, served by github.com outside the API limit), which is reliable for branches with a single run. The T10 badge went "no status" → **passing**. T10 merged into `develop` and pushed. Branch `feat/S01.1-T11-app`; T11 In Progress.
- Design:
  - `run(ctx, args, stdout, stderr)` with subcommands `serve`, `migrate up|status`, `version`, `help`; exit codes 0 OK, 1 runtime/config error, 2 usage. `main` cancels ctx on Ctrl+C/SIGTERM.
  - `setup`: config.Load → logging.New (logs in `<root>/.local-ai-nas/logs`) → db.Open (`<root>/.local-ai-nas/db/nas.db`). An invalid config prints every problem and exits 1.
  - `serve`: migrate on start; `http.Server` with ReadHeaderTimeout and IdleTimeout from config, MaxHeaderBytes 64 KiB, a 1 MiB body limit (upload routes will set their own), ErrorLog into slog. Handler order: RequestID → AccessLog → Recover → MaxBytes → mux. Graceful shutdown with `server.shutdown_timeout`.
  - `internal/health`: named checks with a 2 s timeout each; 200 `ok`, or 503 `fail` with the check errors. Only a `database` check in T11; S01.2-T05 adds the storage checks. The route is hand-written until the spec-first API in S01.5.
  - Not in T11: the loopback-only bind guard (S01.6-T06) and problem+json for 404/405 (S01.5 conventions).

### E037 | 07:21 | TASK DONE S01.1-T11 (app skeleton)
- `cmd/local-ai-nas`: `main.go` (run, subcommands, exit codes 0/1/2, signal context), `serve.go` (setup: config → logging → db; migrate on start; http.Server with ReadHeaderTimeout/IdleTimeout/MaxHeaderBytes 64 KiB/ErrorLog; 1 MiB body limit; graceful `serve()`), `migrate.go` (`up`, `status` table). `internal/health` (Check, Report, Run with a 2 s timeout per check, Handler 200/503). `internal/health/doc.go` removed; the T01 `main_test.go` replaced.
- **Bug found by the tests and fixed:** `http.MaxBytesHandler` passes a *copy* of the request to the next handler. Placed inside the access log, it hid the route pattern the mux sets (the access line logged `route: ""`). The body limit now sits outside the access log (RequestID → MaxBytes → AccessLog → Recover → mux), and AccessLog's doc says that middleware between it and the mux must pass the same request.
- Test round 1 also failed on exact tabwriter spacing in the migrate test (a test bug); it now matches with regular expressions.
- Tests: commands and exit codes (12 cases: version, help, usage, unknown, `-h`, bad flag, extra argument, missing root, relative root, migrate without or with a wrong action); an invalid config message names the key (`config: log.level (from flag --log-level): ...`, exit 1); **in-process serve** (health 200 with the database check, X-Request-ID, POST gives 405, cancel → exit 0; the log file has starting / migration applied / listening / request with the route / shutting down / stopped; the database file exists); port in use → exit 1 "cannot listen"; migrate status → up → up → status; **graceful shutdown waits for an in-flight request**; the shutdown timeout gives an error; a listener error is returned; **smoke test**: the test binary re-runs itself as `main` in a separate process, checks health, then SIGTERM → exit 0 with "stopped" on Linux/macOS, or kill on Windows (no SIGTERM there; graceful shutdown is covered in-process).
- Manual check on Windows: `local-ai-nas version` → "local-ai-nas manual-check"; `serve` created `<root>/.local-ai-nas/{db/nas.db (+ -wal, -shm), logs/nas.log}`; health → 200 `{"status":"ok","version":"manual-check","checks":[{"name":"database","status":"ok"}]}`; the access line has route "GET /api/v1/system/health".
- Checks: `go test ./...` OK; coverage of internal/... 93.8%, cmd 84.4%; golangci-lint 0 issues; licenses exit 0; CGO-free builds linux/amd64, linux/arm64, windows/amd64 OK.

### E039 | 07:36 | CI FAILURE on T11 (Linux) → cause found and fixed
- The T11 branch CI run 35946915940 **failed**: job "Test (ubuntu-latest)", step "go test (race detector)". Windows tests, lint, checks, and builds passed. The job and step names came from the public run and job web pages, because the anonymous REST API was rate-limited until 07:54 and the step logs need sign-in.
- Reproduced locally without the logs: I cross-compiled the Linux test binaries on Windows (`CGO_ENABLED=0 GOOS=linux go test -c`) and ran them in the existing **WSL Ubuntu** distro. `cmd/local-ai-nas` gave "fatal error: all goroutines are asleep - deadlock!" in `TestSmokeBinary`. The other six packages passed on Linux.
- Cause (a test bug, not a server bug): after SIGTERM the test received the process result from a buffered channel, and the deferred cleanup then used `len(waitc) > 0` to decide whether the process had exited. That was false once the value had been read, so the cleanup waited on the channel forever. The Windows path returns before SIGTERM, so it never hit this.
- Fix: a `done` channel that is closed after `cmd.Wait` (with `waitErr` set before the close); `exited()` selects on it. Verified: Windows `go test ./cmd/...` OK; Linux (WSL) all 9 cmd tests pass twice (`-test.count=2`), including the SIGTERM graceful stop with "stopped" in the log; lint clean.
- CI watching from now on: `watch_html.py` (scratchpad) reads the run page on github.com for a given commit (status Queued / In progress / Success / Failure) and, on failure, the failed job and step names. It does not depend on the API limit or on the badge, which shows the previous run while a new one is in progress.

### E038 | 07:39 | TASK START S01.1-T06 (write-ahead)
- T11: branch CI badge **passing** (commit 15b4a27), merged into `develop` and pushed. Branch `feat/S01.1-T06-devenv`; T06 In Progress (the last S01.1 task, moved after T11 in E026).
- Base images checked on Docker Hub (2026-09-24): `golang:1.27.1-trixie` (pushed 2026-09-19) and `debian:trixie-slim` (pushed 2026-09-19), both with amd64 and arm64/v8. Action licenses from their LICENSE files at the pinned tags: docker/setup-buildx-action v4.4.1 **Apache-2.0**, aquasecurity/trivy-action v0.36.0 **Apache-2.0** (its default Trivy is v0.70.0, so the workflow pins `version: v0.74.0`).
- Design:
  - `Dockerfile.dev` stages: `build` (golang, module download, CGO-free build), `dev` (FROM build; `go run ./cmd/local-ai-nas serve` on the source bind-mounted at /src), `runtime` (debian:trixie-slim, non-root user `nas` uid 10001, `/data` volume, ENTRYPOINT the binary).
  - `compose.dev.yaml`: target `dev`, source bind mount `..:/src`, named volumes for data and the Go caches, port `127.0.0.1:8080:8080`, and `LOCALAINAS_SERVER_BIND=0.0.0.0:8080` **inside** the container.
  - **Note for S01.6-T06 (bind guard):** inside a container the server must listen on 0.0.0.0 for the published port to reach it, while the host publishes on loopback only. The S01.6-T06 loopback guard therefore needs an explicit, documented container exception, or the dev compose breaks. This is recorded in the S01 change log.
  - The Docker daemon is not running on the dev PC, so the container part is verified by a new CI job.

### E040 | 07:44 | TASK DONE S01.1-T06 (development environment) + SUBSTAGE S01.1 CLOSED
- T06 files: `deploy/Dockerfile.dev` (build / dev / runtime stages), `deploy/compose.dev.yaml`, `deploy/config.example.toml` (+ `internal/config/example_test.go`: the example parses, sets every setting, and equals the defaults), `.dockerignore`, `deploy/README.md`, the README "Development" section (native run, Docker, tests and checks). CI: a "Quick start from the README" step in both test jobs (ubuntu and windows) and a new `image` job (build the runtime image, run it and check health, start the compose dev environment and check health, Trivy v0.74.0 HIGH/CRITICAL with ignore-unfixed). Dependabot `docker` for `/deploy`. Register: setup-buildx-action and trivy-action are in use (both Apache-2.0); golang:1.27.1-trixie build-image row added.
- Checks by hand on Windows: the README command `go run ./cmd/local-ai-nas serve --storage-root "$PWD/dev/data"` works in Git Bash and in PowerShell (health 200, database ok); `dev/` is git-ignored. The servers were stopped and `dev/` removed afterwards. The config example test passes; lint is clean.
- CI for T06: run **35948272549** green, including the image job (the Docker half of the T06 acceptance is verified there, because the Docker daemon is not running on the dev PC).
- **S01.1 substage acceptance** (plan S01.1): all five criteria are met; the evidence is recorded in the S01 change log. `docs/api/errors.md` documents the error format (criterion 5). S01.1 is **Done** in the stage document and in plan 10.2. Plan status fields are updated without a version bump (tracking only, no content change).

### E041 | 07:48 | S01.1 MERGED; SUBSTAGE S01.2 STARTED; TASK START S01.2-T01 (write-ahead)
- The S01.1 closure commit's CI run 35948534298 was green; `feat/S01.1-T06-devenv` merged: `develop` = 656301f (pushed).
- S01.2 In Progress (stage document and plan 10.2); branch `feat/S01.2-T01-layout`; T01 In Progress.
- Design: package `internal/storage`, with `Layout` (root, internal dir, tmp/uploads, db, logs; T02 adds relocation) and `Init()`, which creates the missing directories (0750), checks that each layout directory is a real directory (not a file or a symlink) and writable (a probe file, removed afterwards, with the directory's modification time restored, so a second start changes nothing a user can see), and returns the unknown root entries, which it leaves untouched. `serve` and `migrate` call it after loading the config and before logging starts; unknown entries are logged as a warning. The temporary `.local-ai-nas` constant in `serve.go` goes away.

### E042 | 07:51 | TASK DONE S01.2-T01 (layout initializer)
- `internal/storage/layout.go`: constants (files, photos, .local-ai-nas, u0001), `Layout` (Root, Internal, TmpUploads, DB, Logs), `NewLayout`, `Area`, `Init` (absolute root required; MkdirAll root; ensureDir for each layout directory: create with 0750, or reject a file or a symbolic link; checkWritable: a probe file, then the directory's modification time restored; unknown root entries returned sorted and never touched).
- `cmd/local-ai-nas/serve.go`: `setup` runs `Layout.Init` after the config and before logging; a layout error prints to stderr and exits 1; unknown entries are logged as a warning naming them. The temporary `internalDirName` is gone; logs and db paths come from the layout.
- Tests (`internal/storage/layout_test.go`): full layout from an empty (and not yet existing) root; **a second Init changes nothing** (the same directories and modification times; user files untouched); unknown entries reported and untouched; a file where `files`, `photos/u0001`, or `.local-ai-nas/db` should be is rejected; a symlinked area is rejected; a relative root is rejected; **a read-only root fails with "cannot create"** and a read-only area with "... is not writable". Read-only is set with `chmod 0555` on Unix (skipped as root) and with an `icacls /deny <user>:(W)` ACL entry on Windows (removed at cleanup), as the task requires. cmd tests: unknown root entries are warned about and left alone; a layout error stops `serve` with exit 1.
- Results: Windows: all packages OK (the symlink test is skipped: no symlink privilege on this PC); **Linux (WSL, user khizir)**: storage and cmd test binaries all PASS, including the symlink and chmod cases; coverage of internal/... 93.1%; golangci-lint 0 issues.

### E043 | 07:55 | TASK START S01.2-T02 (write-ahead)
- S01.2-T01: CI run 35948981464 green; merged: `develop` = 843c6fd (pushed).
- Follow-up on T06 (API available again after 07:54): run 35948272549 had all 8 jobs **success**, including "Test (windows-latest)" with the README quick-start step **success** and "Test (ubuntu-latest)" quick start **success**. This confirms the S01.1 acceptance criterion 2 on both OSes in CI.
- Branch `feat/S01.2-T02-internal-data`; T02 In Progress.
- Design:
  - config gets `storage.db_dir` and `storage.logs_dir` (optional; empty = `<root>/.local-ai-nas/db` or `/logs`; if set, an absolute path).
  - `storage.Options{DBDir, LogsDir}` goes into `NewLayout`. `Init` validates before creating anything. No internal directory (internal root, tmp/uploads, db, logs) may equal, contain, or sit inside the `files` or `photos` area. db and logs must not overlap tmp/uploads. A relocated db or logs directory inside the storage root must be inside `.local-ai-nas`, to keep the root to the two areas plus internal data (I1/I2). `tmp/uploads` can never be relocated. Paths compare case-insensitively on Windows and macOS.
  - Relocated directories are created with MkdirAll.

### E044 | 07:57 | TASK DONE S01.2-T02 (internal data location)
- config: `storage.db_dir`, `storage.logs_dir` (strings, default empty; if set they must be absolute; cleaned). `deploy/config.example.toml` documents both (the example test still passes: every setting present, equal to the defaults).
- storage: `Options{DBDir, LogsDir}`; `NewLayout(root, opts)`. `Init` first calls `validate()`, which runs before anything is created:
  - absolute paths;
  - `noOverlap` for each internal directory (internal root, uploads, db, logs) against each area (the same directory / inside / contains);
  - db and logs against uploads;
  - a relocated db or logs directory inside the root must be under `.local-ai-nas`.
  `within`/`samePath` compare case-insensitively on Windows and macOS. Relocated directories are created with MkdirAll.
- cmd: `serve`/`migrate` pass the two settings to the layout.
- Tests:
  - **One per overlap case** (10): db inside the files area; logs inside the photos area; db = the files area; logs = the photos area; an area inside the logs directory (logs = root); an area inside the db directory (db = the root's parent); db = uploads; logs contain uploads; db elsewhere in the root; a relative db directory. Each asserts the exact message and that **nothing was created**.
  - A valid relocation (db outside the root with missing parents, logs inside `.local-ai-nas`; the default db directory is not created).
  - `within` (same path, child, a sibling with the same prefix, parent, the filesystem root, case-insensitive on Windows).
  - config: relative db/logs directories rejected; cleaning.
  - cmd: `migrate up` with `--storage-db-dir` puts nas.db there; an overlapping `--storage-logs-dir` stops `serve` with exit 1 and the overlap message.
- Results: Windows all packages OK; **Linux (WSL)** storage, config, and cmd PASS; coverage of internal/... 93.6%; golangci-lint 0 issues.

### E045 | 08:01 | TASK START S01.2-T03 (write-ahead)
- S01.2-T02: CI run 35949441925 green; merged into `develop` and pushed. Branch `feat/S01.2-T03-resolver`; T03 In Progress.
- Design (drafted while T02's CI ran):
  - `storage.Resolver`: `Resolve(area, namespace, userPath)` gives a clean slash path relative to the namespace (`/` → `.`). It rejects: no leading `/` (invalid_request); a NUL or a backslash (invalid_name); any `..` segment, even one that stays inside (outside_root); names that `filepath.IsLocal` refuses on this OS, such as NUL or C: on Windows (invalid_name); the photos area (not_available, S01.2-T06); an unknown area or a malformed namespace (invalid_request).
  - `OpenRoot(area, namespace)` gives the os.Root of the namespace directory, the second layer. S01.6-T01/T02 extend the rules with the attack corpus and the name rules.
  - `files.Item` (OwnerID, Area, RelPath, Name, Kind, Size, ModTime, MIME, ETag) and `NewItem(namespace, relPath, info)`.
  - **Architecture test** in `internal/files`: it parses the package's non-test sources and fails on path-based file-system calls (`os.Open/ReadFile/Stat/Rename/...`, `os.OpenRoot` (the resolver opens roots), `filepath.Walk/Glob/...`) and on imports of `io/ioutil`, `syscall`, `x/sys`. A second test proves the check catches a violating snippet.

### E046 | 08:01 | TASK DONE S01.2-T03 (namespace resolver and owner model)
- `internal/storage/resolver.go`: `Resolver`, `NewResolver`, `Resolve`, `OpenRoot`, `checkNamespace` (namespace pattern `^u[0-9]{4}$`). Errors are apperr kinds (invalid_request, invalid_name, outside_root, not_available, internal).
- `internal/files/item.go` (the package doc says all file I/O goes through the resolver and os.Root): `Kind` (file, dir, symlink, other), `Item`, `NewItem`.
- `internal/files/arch_test.go`: **the architecture test** (`TestNoDirectFileSystemAccess`: parses every non-test source and rejects path-based os/filepath calls, `os.OpenRoot`, and imports of io/ioutil, syscall, x/sys) plus `TestViolationsDetected` (a snippet with `os.ReadFile`, `filepath.Walk` under an alias, and `syscall` gives exactly those three findings; `os.Getpid` is allowed).
- Tests: Resolve mappings (9, including `/` → `.`, cleaning, Unicode, dotted names); rejections with their kinds (16 on all OSes, including the photos area being unreachable through the files area both by `..` and by kind; plus `/NUL` and `/C:/Windows` on Windows); OpenRoot (reads through the root; photos → not_available; a missing namespace → internal); **second layer**: with the resolver bypassed, os.Root refuses `../../photos/...` reads, `../` writes, and a symlink out of the namespace (Linux; skipped on Windows without the symlink privilege); NewItem for the root, a folder, a file (OwnerID u0001), and a symlink.
- Results: Windows OK; Linux (WSL) storage and files PASS, with no skips; golangci-lint 0 issues; resolver.go 100% covered; internal/... 93.6%.
- S01.2 acceptance 3 ("every resolved item carries an owner and a namespace, and no request can address outside its namespace") is covered at the storage level. It is enforced end to end once the FilesService (S01.3) uses the resolver.

### E047 | 08:05 | TASK START S01.2-T04 (write-ahead)
- S01.2-T03: CI run 35949731329 green; merged into `develop` and pushed. Branch `feat/S01.2-T04-free-space`; T04 In Progress.
- Design (drafted while T03's CI ran): `DiskFree(dir)`: `unix.Statfs` (Bavail × Bsize) under `//go:build unix`, `windows.GetDiskFreeSpaceEx` (bytes available to the user, quotas included) under `//go:build windows`. `SpaceGuard{dir, reserve, free FreeFunc}`: `Check(size)` → insufficient_storage (507) when `free - size < reserve`, invalid_request for a negative size, internal when the provider fails; `Free()`, `Reserve()`. The reserve comes from `storage.free_space_reserve` (default 1 GiB, T07). Integration test: a handler that checks the guard before creating the file answers 507 with a problem body, and no file exists afterwards. golangci-lint also runs with GOOS=linux locally, so the unix-only file is linted here too.

### E048 | 08:07 | TASK DONE S01.2-T04 (free-space guard)
- `internal/storage/space.go` (FreeFunc, SpaceGuard, NewSpaceGuard, Free, Reserve, Check, formatBytes), `space_unix.go` (DiskFree via `unix.Statfs`), `space_windows.go` (DiskFree via `windows.GetDiskFreeSpaceEx`). `golang.org/x/sys v0.48.0` is now a direct requirement (register updated, R6); the license check exits 0.
- Tests: DiskFree on a real directory (> 0) and on a missing one (error); Check at 0, 5 GiB, exactly the reserve left (allowed), 1 byte over, all free space, more than is free, and a negative size, with the message ("not enough free space: the write needs 9.0 GiB, 10.0 GiB is free, and 1.0 GiB must stay free") and status 507; a provider error → internal with the cause; defaults (a negative reserve counts as 0, nil uses DiskFree); formatBytes; **integration**: a handler with the guard in front of an os.Root write: 100 bytes → 201 and stored; 101 bytes over the injected free space → **507** `application/problem+json` code `insufficient_storage`, and **no file on disk**.
- Results: Windows OK; `go vet` for windows, linux, darwin; golangci-lint 0 issues on Windows and with GOOS=linux; Linux (WSL) free-space tests PASS; coverage of internal/... 93.8%.
- The guard is not used by a handler yet: the upload handlers (S01.3-T05, S01.4-T02) call it, and the health check (T05) reports free space against the reserve.

### E049 | 11:19 | USER + TASK START S01.2-T05 (write-ahead)
- User message: "continue" (after the T04 CI notification). Continuing with the S01 execution order.
- S01.2-T04: CI run 35950125517 green; merged into `develop` and pushed. Branch `feat/S01.2-T05-health`; T05 In Progress.
- Design:
  - `storage.DeviceID(path)`: `unix.Stat` st_dev on Unix; on Windows the volume serial from `GetFileInformationByHandle` (the directory opened with FILE_FLAG_BACKUP_SEMANTICS). `DeviceFunc` type. `Layout.FinalizeMode(dev)` gives "rename" when `tmp/uploads` and `files/<ns>` share a device, otherwise "copy" (plan 8.18; used by S01.4).
  - `internal/health`: a check returns (detail, error). A `*health.Warning` error means **warn** (degraded but working); any other error means fail. The report status is fail > warn > ok; HTTP 503 only on fail. `checks.go` has named checks: `config`, `storage_writable` (the layout's write probe), `same_filesystem` (warn + `upload_finalize_mode=copy` on a mismatch; injectable DeviceFunc), `free_space` (fail below the reserve; the detail shows free and reserve), `database` (ping + no pending migrations).
  - `serve`: the checks run once at startup (each warn/fail logged; startup continues, because the fatal conditions already stopped `setup`) and on every `GET /api/v1/system/health`.

### E050 | 11:23 | TASK DONE S01.2-T05 (startup health checks)
- storage: `device_unix.go` / `device_windows.go` (`DeviceID`), `filesystem.go` (`DeviceFunc`, `FinalizeRename`/`FinalizeCopy`, `Layout.FinalizeMode`, `Layout.Writable`); `layout.go`: `ensureExistingDir` split out of `ensureDir`, because a health check must never create directories.
- health: `health.go` (Check.Run returns (detail, error); `Warning`/`Warnf`; statuses ok/warn/fail; the report status is fail > warn > ok; 503 only on fail; a `detail` field), `checks.go` (Config, StorageWritable, SameFilesystem, FreeSpace, Database).
- cmd: `app.layout`; `serve` builds a `SpaceGuard` (reserve from config) and the 5 checks, runs them once at startup (`logStartupChecks`: fail → error, warn → warn, ok → debug, plus a "startup checks" summary line), and serves them on `GET /api/v1/system/health`.
- Tests: **each failing condition by name**: storage_writable (a layout never created; the storage test also uses a read-only logs dir), **same_filesystem mismatch simulated with an injected DeviceFunc → warn with "upload_finalize_mode=copy"**, a device error → fail, free_space below the reserve → fail (and a provider error), database with pending migrations → fail, ok after migrating, closed → fail; config with and without a file. DeviceID (the same ID for two paths in one temp dir; a missing path → error); FinalizeMode (real rename; injected same/different/error); Writable (before Init fails, after Init ok and leaves no files, read-only logs fails); report aggregation (warn keeps 200; fail wins → 503); Warning. The cmd in-process test expects the 5 checks, all ok, and the "startup checks" log line.
- A heredoc once more turned `\n` into a newline in a Go string (caught by the compiler; fixed with Edit). Code with backslashes goes through Write/Edit only.
- Results: Windows all packages OK; **Linux (WSL)** storage, health, cmd PASS; golangci-lint 0 issues (windows + GOOS=linux); coverage of internal/... 94.0%.
- Manual Windows run: health 200, all 5 checks ok (55,184,162,816 bytes free, reserve 1 GiB, upload_finalize_mode=rename, 1 migration applied); log: "startup checks" status ok.

### E051 | 11:26 | TASK START S01.2-T06 (write-ahead)
- S01.2-T05: CI run 35964271426 green; merged into `develop` and pushed. Branch `feat/S01.2-T06-photos-placeholder`; T06 In Progress.
- Already in place from earlier tasks: `photos/u0001` is created and validated by `Layout.Init` (T01); the resolver refuses the photos area (`not_available`) and every path from the files area into it (`outside_root`), and os.Root refuses it even when the resolver is bypassed (T03 tests).
- Remaining: register `/api/v1/photos` and `/api/v1/photos/` (all methods, all sub-paths) to answer 501 `not_available` as a problem with the correlation ID. The route conventions document (S01.5-T01) records it as reserved.

### E052 | 11:27 | TASK DONE S01.2-T06 + SUBSTAGE S01.2 CLOSED
- T06: `serve.go` registers `/api/v1/photos` and `/api/v1/photos/` (all methods) → `apperr.Write(not_available)`. `cmd/local-ai-nas/routes_test.go`: GET/POST/DELETE on 5 photos paths → 501 `application/problem+json`, code `not_available`, correlation_id = X-Request-ID. The resolver side (photos unreachable) was already tested in T03. Windows and Linux (WSL) cmd tests PASS; lint clean.
- **S01.2 acceptance** (plan S01.2), all 5 criteria met; the evidence is in the S01 change log. S01.2 is **Done** in the stage document and in plan 10.2 (a status field only).
- Next (execution order): S01.6-T01, S01.6-T02, then S01.5-T01..T04.

### E053 | 11:31 | SUBSTAGE S01.6 STARTED; TASK START S01.6-T01 (write-ahead)
- S01.2 closure: CI run 35964600803 green; merged into `develop` and pushed.
- S01.6 In Progress (stage document and plan 10.2); branch `feat/S01.6-T01-paths`; T01 In Progress.
- **NFC decision (the task asks for one):** **normalize** names to NFC with `golang.org/x/text/unicode/norm` v0.42.0 (the register already listed it as the conditional candidate), instead of rejecting non-NFC names. Why: macOS clients (and WebDAV from S09) commonly send decomposed (NFD) names. NFC and NFD of the same text are canonically equivalent (the same visible name), so normalizing is not a silent rewrite of an *invalid* name (the S01.6-T02 principle). Without it, Linux would keep "é" (NFC) and "é" (NFD) as two different files that look identical.
- Path rules added: exactly one leading "/" (`//server/share` refused); percent-encoded `%2F`, `%5C`, `%00` refused (a double-encoded path); segments of three or more dots, or dots mixed with spaces, refused (Windows may treat them as ".."); drive-letter segments (`C:`) refused. The T03 rules (NUL, backslash, "..", IsLocal) stay.
- Attack corpus: 56 cross-platform cases (+5 Windows-only reserved names), used as the test table and as FuzzResolve seeds; a bypass test sends the corpus straight to os.Root and checks that nothing appears outside the namespace. The T03 case `//docs//a.txt` (previously cleaned) is now refused as UNC-like; the test uses `/docs//a.txt`.

### E054 | 11:35 | TASK DONE S01.6-T01 (path normalization and traversal prevention)
- `internal/storage/resolve_path.go`: `cleanUserPath` (the six rules; NFC through `norm.NFC.String`), `isDotsAndSpaces`, `isDriveLetter`. `Resolver.Resolve` = checkNamespace + cleanUserPath. `golang.org/x/text v0.42.0` is now direct (register updated with the decision).
- `internal/storage/attack_test.go`: `attackCorpus` (**56 cases**: not API paths, parent segments, other namespaces/photos/internal data, dot runs and dot-space mixes, backslashes, UNC and device paths, drive letters, percent-encoded separators and NUL, raw NUL) + `windowsOnlyCorpus` (NUL, con, COM1, LPT1). Tests: the corpus size is at least 50; every corpus path is refused (and the Windows-only ones on Windows); NFD → NFC; **the corpus sent straight to os.Root** (resolver bypassed): no read of the photos file, and nothing new outside `files/u0001` (the parent, root, areas, photos namespace, and internal data are checked); `FuzzResolve` (seeds = corpus + good paths; an accepted path is local, has no `..`, is NFC, and resolves to itself again).
- Findings while testing on Windows: (1) Go 1.27's `filepath.IsLocal` accepts `aux.txt` (current Windows allows reserved names with an extension), so it moved out of this layer; S01.6-T02 refuses reserved names on every OS. (2) Given a corpus path such as `...`, os.Root on Windows created an entry inside the namespace that cannot be opened again. That confirms the dot-run rule, and the bypass test now checks the areas directly instead of walking the namespace.
- Results: Windows OK; **Linux (WSL)** corpus, NFC, bypass, and fuzz seeds PASS; a **60 s fuzz run: 3,487,578 execs, 479 corpus entries, no failure**; golangci-lint 0 issues (windows + linux); licenses OK; resolve_path.go 100% covered; internal/... 94.2%.

### E055 | 11:38 | TASK START S01.6-T02 (write-ahead)
- S01.6-T01: CI run 35965241227 green; merged into `develop` and pushed. Branch `feat/S01.6-T02-names`; T02 In Progress.
- Design (drafted while T01's CI ran):
  - `storage.ValidateName(name)` for every name the API is about to create (upload, new folder, rename/move/copy target) and `ValidateNewPath(rel)` (the path length plus each component). **Existing names on disk are not checked**, so files put there by other means stay readable; this is why the rules are separate from `Resolve`.
  - Rules and IDs (in the problem's new `rule` field; the code stays `invalid_name`): empty_name, dot_name (`.`, `..`), reserved_name (CON, PRN, AUX, NUL, CONIN$, CONOUT$, COM0–9, LPT0–9, COM¹²³, LPT¹²³; with any extension, any case, trailing spaces before the extension; on every OS), forbidden_character (`<>:"/\|?*`), control_character (U+0000–U+001F and U+007F), trailing_dot_or_space, name_too_long (> 255 UTF-8 bytes), path_too_long (> 4096 bytes), invalid_utf8.
  - Policy notes: 255 bytes is the common file-system name limit; 4096 bytes for the path inside a namespace (Go handles long Windows paths; very long paths may still be awkward for Windows clients over SMB, to revisit in S09).
  - apperr: `Error.Rule`, `NewRule(kind, rule, detail)`; `Problem.Rule` (`rule`, omitted when empty), set for non-internal errors. `docs/api/errors.md` gets the field and the rule table.

### E056 | 11:42 | TASK DONE S01.6-T02 (file name rules)
- `internal/storage/names.go`: `MaxNameBytes` 255, `MaxPathBytes` 4096, 9 rule IDs, `ValidateName`, `ValidateNewPath`. `internal/apperr`: `Error.Rule`, `NewRule`, `Problem.Rule` (`rule`, omitted when empty; never set for internal errors). `docs/api/errors.md`: the `rule` field and a "Name rules" section (the rule table and the NFC note).
- Tests: `TestValidateName` (58 cases: 11 accepted including CONSOLE.txt, com10, and 255-byte and 254-byte names; then every rule with variants: empty, `.`/`..`, 15 reserved forms including `nul .txt`, COM¹, LPT³.txt, CONIN$; 10 forbidden characters including the `file.txt:stream` ADS form; 5 control characters; 4 trailing dot/space; 2 too long; invalid UTF-8), `TestValidateNewPath` (7 cases, including a 4222-byte path), `TestNameRuleInProblem` (aux.txt → 400 invalid_name / reserved_name), apperr `ProblemFor` with a rule and with an internal rule (hidden). **`FuzzValidateName`**: any accepted name can be created, is listed under the same name (up to NFC), and reads back.
- Results: the whole suite is OK on Windows; **Linux (WSL)** name tests and the whole storage package PASS; **60 s fuzz on Windows/NTFS: 136,384 execs, no failure**; golangci-lint 0 issues (windows + linux); names.go 100% covered; internal/... 94.4%.
- S01.6-T01 and T02 are done; S01.6 stays In Progress (T03–T06 come after S01.3 in the execution order). Next: S01.5-T01..T04.

### E057 | 11:46 | SUBSTAGE S01.5 STARTED; TASK START S01.5-T01 (write-ahead)
- S01.6-T02: CI run 35965767428 green; merged into `develop` and pushed.
- S01.5 In Progress (stage document and plan 10.2). The S01 change log records the adjusted split: routes move into `internal/api` (T02); the oapi-codegen generation pipeline moves from T05 into T04; 404/405 become problems (T03).
- Branch `docs/S01.5-T01-conventions`; T01 In Progress. Drafted: `docs/api/conventions.md` (base path and areas, namespaces, naming, path addressing, requests and responses, pagination and sorting, `on_conflict`, errors, request IDs, tus metadata keys, a 7-point review checklist and its application to the existing endpoints).

### E058 | 11:46 | TASK DONE S01.5-T01 (route conventions)
- `docs/api/conventions.md` committed; `docs/README.md` lists it (and versioning.md, which comes with T02).
- The review checklist was applied to the two existing endpoint groups. The results are recorded honestly in the document: health and the reserved photos routes meet items 1, 3, 4, and 7 (5 and 6 have nothing to check); **item 2 (in the spec) is not yet met** for either and is scheduled for S01.5-T03/T04. The file endpoints get reviewed when they are added (S01.3, S01.4).
- Decisions written down there: path in the `path` query parameter (never in the URL path); snake_case; RFC 3339 UTC times; unknown JSON fields refused; cursor pagination (limit 100 default, 1000 max; sort name/size/mod_time/kind; tie-break by name); on_conflict fail/rename/overwrite (folders never merged or overwritten); 404/405 as problems; tus metadata keys `target_path`, `on_conflict`, `sha256`.

### E059 | 11:50 | TASK START S01.5-T02 (write-ahead)
- S01.5-T01: CI run 35966117590 green; merged into `develop` and pushed. Branch `feat/S01.5-T02-versioning`; T02 In Progress.
- Plan: `docs/api/versioning.md` (the path version; compatible vs breaking changes; deprecation with the `Deprecation` (RFC 9745) and `Sunset` (RFC 8594) headers and at least two minor releases or six months; a pre-release note until S11.7). `internal/api/api.go`: `Options`, `Route`, `Routes` (the table: health, photos reserved), `New` (the middleware chain moved from `cmd`). The test `TestRoutesAreVersioned` checks every pattern's path is under /api/v1 or /api/docs; `TestUnversionedPathsAreNotServed`. `cmd/local-ai-nas/serve.go` uses `api.New`; `routes_test.go` moves into `internal/api`.

### E060 | 11:51 | TASK DONE S01.5-T02 (versioning policy + route table)
- `docs/api/versioning.md` committed. `internal/api/api.go` replaces `internal/api/doc.go`: `DefaultMaxBodyBytes`, `Options`, `Route`, `Routes`, `New`. `cmd/local-ai-nas/serve.go`: `newHandler` removed; `serve` calls `api.New(api.Options{Logger, Version, Checks})`. The photos test moved from `cmd` to `internal/api`.
- Tests (`internal/api/api_test.go`): **`TestRoutesAreVersioned`** (every route's path is /api/v1…, or /api/docs…), `TestRoutePath` (patterns with a method and a host), `TestUnversionedPathsAreNotServed` (/, /health, /api, /api/v2/..., /v1/... → 404), `TestHealthRoute`, `TestPhotosAPINotAvailable`. The cmd in-process and smoke tests still pass (the same behavior through the new package).

### E061 | 11:55 | TASK START S01.5-T03 (write-ahead)
- S01.5-T02: CI run 35966560636 green; merged into `develop` and pushed. Branch `feat/S01.5-T03-problem-schema`; T03 In Progress.
- Design (drafted while T02's CI ran):
  - apperr: a new kind `MethodNotAllowed` (405, `method_not_allowed`), appended at the end, and `Kinds()` listing all kinds.
  - `internal/api/unmatched.go`: `problemsForUnmatched(mux)` asks `mux.Handler(r)` for the pattern. When there is none, it runs the mux's own handler into a probe writer to learn 404 vs 405 and the `Allow` header, then answers with a problem (`not_found`, or `method_not_allowed` with `Allow`). A matched request goes through `mux.ServeHTTP` unchanged, so path values and `r.Pattern` still work.
  - `api/openapi.yaml` (info.version 0.1.0, license): the schemas `Problem` (additionalProperties false; code enum = all apperr codes), `HealthReport`, `HealthCheck`, `HealthStatus`; the responses NotFound, MethodNotAllowed (with Allow), NotAvailable, InternalError; the paths `/system/health` (200/503 report, 405, 500) and `/photos` (501).
  - Contract test (`internal/api/contract_test.go`): the spec loads and validates (kin-openapi); every error case (404s, 405s with Allow, 501 photos with 4 methods, a 500 from a panic) is validated against the Problem schema, with status = HTTP status and correlation_id = X-Request-ID. **Every route in the table must be covered by at least one error case** (so new endpoints cannot skip it). The spec's code enum equals `apperr.Kinds()`, and health validates against HealthReport.
  - Test-only dependency: `github.com/getkin/kin-openapi` (already in the module graph through oapi-codegen); it becomes a direct requirement for tests only and is not linked into the binary.

### E062 | 11:57 | TASK DONE S01.5-T03 (error catalogue and problem schema)
- Code: apperr `MethodNotAllowed` (405) + `Kinds()`; `internal/api/unmatched.go`; `api.New` wraps the mux with it. `api/openapi.yaml` as designed in E061. `docs/api/errors.md`: method_not_allowed, not_found also for unknown endpoints, and a pointer to the spec and the contract test. `docs/api/conventions.md`: the review table now counts checklist item 2 as met for health and photos (in the spec). `go.mod`: `github.com/getkin/kin-openapi v0.142.0` (tests only; register section 3).
- Tests: `TestErrorResponsesMatchSchema` (10 cases: 3× 404, 2× 405 with Allow, 5× 501 photos across GET/POST/PUT/DELETE and sub-paths; every route of the table is covered), `TestPanicResponseMatchesSchema` (500), `TestEveryCodeIsInTheSpec`, `TestHealthMatchesSchema`, `TestKinds`, plus the kind table with method_not_allowed.
- **Negative check:** with `not_found` removed from the spec's code enum, the contract test failed on the three 404 cases ("value is not one of the allowed values") and `TestEveryCodeIsInTheSpec` failed; the spec was then restored from the draft and the tests pass again. (My first restore attempt copied from a backup path that was not written, so the broken spec stayed for one command; I caught it through the failing final test run and restored from the draft.)
- Results: the whole suite is OK on Windows; the Linux (WSL) api package PASS (run from the package directory); golangci-lint 0 issues (windows + linux); licenses OK (kin-openapi not linked); internal/... 94.5%.

### E063 | 12:01 | TASK START S01.5-T04 (write-ahead) + an incident on the dev PC
- S01.5-T03: CI run 35967063660 green; merged into `develop` and pushed. Branch `feat/S01.5-T04-generated-server`; T04 In Progress.
- **Incident (my mistake, 11:58):** for a trial generation I put a Git Bash path (`/c/Users/.../scratchpad/gentry/gen/api.gen.go`) into an oapi-codegen config file. oapi-codegen is a native Windows program, so it read the path from the drive root and **created `C:\c\Users\...\gentry\gen\api.gen.go`** (one 12,980-byte generated file plus its parent directories, all created at 11:58:28, nothing else in `C:\c`, checked by listing). Removing `C:\c` was **blocked by a safety check** ("protected from removal"). I did not work around it; **the user has been asked to delete `C:\c`** (it contains only that generated file). Lesson: paths inside config files read by native Windows tools must be Windows-style (`cygpath -m`); the trial generation was then redone correctly into the scratchpad.
- Trial generation results (v2.8.0, config `package: gen`, `models`, `std-http-server`, `strict-server`, `exclude-tags: [photos]`): 454 lines; `ServerInterface`, `ServerInterfaceWrapper{Handler, HandlerMiddlewares, ErrorHandlerFunc}`, `StrictServerInterface.GetHealth`, response types `GetHealth200/503JSONResponse` and `405/500` problem responses, and enum `Valid()` methods. Its imports are standard library only for now; `github.com/oapi-codegen/runtime` is needed once operations have parameters, so it is added (with its register entry) in the first S01.3 commit that needs it.
- Design: see the CURRENT_STATE write-ahead. `health.Handler` is removed (the generated strict handler is the single implementation); the `Cache-Control: no-store` that it set becomes an API-wide default header.

### E064 | 12:04 | TASK DONE S01.5-T04 (request validation + generated server)
- Files: `internal/api/oapi-codegen.yaml` (models, std-http-server, strict-server; exclude-tags photos); `internal/api/gen/api.gen.go` (generated, v2.8.0); `internal/api/server.go` (`//go:generate`, `server` implementing `gen.StrictServerInterface` with a compile-time assertion, `GetHealth` mapping the health report to 200/503, `generated()` with the hooks `bindingError` / `bodyError` (400 invalid_request) and `operationError` (the apperr kind; 500 generic otherwise)); `api.go` (the health route uses the generated wrapper; the `noStore` middleware sets `Cache-Control: no-store`); `health.Handler` removed (health tests now test `Run`; the HTTP side is tested in api).
- Tests: `TestGetHealthStatuses` (ok/warn → 200, fail → 503, no-store), `TestErrorHooks` (binding → 400 invalid_request naming the parameter; body → 400; an operation's conflict → 409; a plain error → a generic 500 without internal text), **`TestSpecOperationsAreRouted`** (every non-photos spec operation has the same METHOD + /api/v1 path in the route table and vice versa), **`FuzzAPI`** (arbitrary method/target/body against the whole API: never a 5xx except photos' 501; every error is `application/problem+json`; seeds come from the route table, so new endpoints are fuzzed automatically).
- Lint round 1: staticcheck QF1001 (De Morgan) in the fuzz test, fixed. Coverage: the generated package showed 0% (its code runs from the api tests, and Go counts coverage per package), which pulled the total to 83.8%; **`scripts/coverage.sh` now leaves out `internal/api/gen`** (documented there and in docs/testing.md) → 94.7%.
- Results: all packages OK on Windows; **drift check** (`go generate ./...` + `git status`): no changes; **FuzzAPI 60 s: 2,237,982 execs, no failure**; golangci-lint 0 issues (windows + linux); licenses OK; Linux (WSL) api, health, cmd PASS.
- Acceptance: "fuzz over query and body inputs never produces a 5xx" is met for every current route. "Never reaches the service with invalid data" is checked per endpoint with fake services, starting with S01.3 (recorded in the stage row). The T05 remainder is the spec complete for S01 + the CI drift check verified once.

### E065 | 12:09 | USER + SUBSTAGE S01.3 STARTED; TASK START S01.3-T01 (write-ahead)
- User messages: a progress summary was given at the S01.5-T04 checkpoint (including the request to delete `C:\c`); the user answered "continue".
- S01.5-T04: CI run 35967714415 green; merged into `develop` and pushed.
- S01.3 In Progress (stage document and plan 10.2). Branch `feat/S01.3-T01-files-service`; T01 In Progress.
- Design (drafted while the T04 CI ran):
  - `files.Service` grows with each S01.3 task and its endpoint; T01 gives it `Stat`.
  - `Hooks` (`Before` can veto, and then `After` is not called; `After` gets the outcome); `NopHooks`.
  - Paths are resolved **before** the hooks run, so hooks only ever see clean namespace paths.
  - `Local` does I/O only through the resolver's `os.Root`; `fsError` maps not-found / exists / anything else to not_found / conflict / internal.
  - `storage.IsNotFound` also covers a path through a file (ENOTDIR on Unix; ERROR_DIRECTORY / ERROR_INVALID_NAME on Windows), because `internal/files` may not import syscall.
  - The API gets the service as `Options.Files` (cmd builds `files.NewLocal`).
  - A depguard rule keeps `os`, `path/filepath`, `io/ioutil`, `syscall`, and `x/sys` out of `internal/api`, and an architecture test checks the same plus "no `files.NewLocal`/`files.Local` in the API".

### E066 | 12:11 | TASK DONE S01.3-T01 (FilesService interface, local implementation, hooks)
- Files: `internal/files/service.go`; `internal/storage/notfound_{unix,windows}.go` (+ `notfound_test.go`); `internal/api/api.go` (`Options.Files`); `cmd/local-ai-nas/serve.go` (`files.NewLocal(storage.NewResolver(layout), nil)`); `.golangci.yml` (the depguard rule `api-no-disk` for the non-test files of internal/api: os, path/filepath, io/ioutil, syscall, x/sys); `internal/api/arch_test.go`.
- Tests: Stat for the root, a folder, and a file (owner u0001); Stat errors (not_found for missing and through-a-file; outside_root for another namespace and for the photos area; invalid_request; invalid_name; a missing namespace → internal); **hook order** (before, after, before, after; the event carries op/owner/resolved path; After gets nil, then not_found); a Before veto stops the operation and skips After; hooks never run for invalid paths; NopHooks; fsError mapping (not_found, conflict, internal; cause kept; the internal problem stays generic); IsNotFound (missing, through a file; not for other errors).
- Round 1: a missing test directory (`files/u0002` must exist before WriteFiles) and gofmt alignment, both fixed. Lint round: the new depguard rule also hit the API's test files, which read the spec and sources from disk on purpose → the rule now excludes tests (`!$test`).
- **Deliberate violation:** `internal/api/zz_violation.go` importing `os` was flagged by depguard ("not allowed from list 'api-no-disk'") and by `TestHandlersUseOnlyTheService`; the file was removed.
- Results: all packages OK on Windows; Linux (WSL) files, storage (incl. IsNotFound), api PASS; golangci-lint 0 issues (windows + linux); coverage 94.8%.

### E067 | 12:15 | TASK START S01.3-T02 (write-ahead)
- S01.3-T01: CI run 35968332439 green; merged into `develop` and pushed. Branch `feat/S01.3-T02-list`; T02 In Progress.
- Design (drafted while T01's CI ran):
  - `files.List(owner, path, ListOptions{Cursor, Limit, Sort, Order})` gives a `ListPage{Folder, Items, NextCursor}`. Options are normalized and validated (limit 1–1000, default 100; sort name|size|mod_time|kind; order asc|desc). Order: key, then name (case-insensitive, then exact), with Desc reversing the whole total order; kind order dir < file < symlink < other. The cursor is base64url JSON of the last item's position plus the sort and order; a mismatched or garbled cursor → invalid_request. The folder is read with `os.Root.Open` + `ReadDir` + `DirEntry.Info` (Lstat), and entries that vanish meanwhile are skipped.
  - API: `GET /api/v1/files/items?path=&cursor=&limit=&sort=&order=`. A file gives `{item}` (its details); a folder gives `{item, items, next_cursor}`. The parameters are validated in the handler before any service call (enum `Valid()`, limit range).

### E068 | 12:20 | TASK DONE S01.3-T02 (list a folder)
- Code: `internal/files/list.go` (SortKey, Order, DefaultLimit/MaxLimit, ListOptions.normalize, List, readFolder, compareNames, compareItems, cursor); `Service.List`; spec `GET /files/items` with the `Path` parameter, `FileItem`, `ItemKind`, `ItemsResponse`, `ListSort`, `ListOrder`, `BadRequest`, and the files tag; regenerated `internal/api/gen`; `internal/api/files.go` (`GetItems`, `listOptions`, `fileItem`, `noFiles`); `server` gets `files` and `owner` (u0001 in S01); the route table gets `GET /api/v1/files/items`.
- Dependency (R6): generated query binding needs `github.com/oapi-codegen/runtime v1.7.0`, now in go.mod and linked; it also links go-jsonmerge v2.0.0 (MIT) and google/uuid v1.6.0 (BSD-3-Clause). Register updated; license check exit 0.
- Tests: files: **`TestListLargeFolderPaginates`** (10,000 files with mixed-case names, repeating sizes and times, plus 20 folders; for all 4 keys × 2 orders, pages of 333: no duplicates, no gaps, 31 pages, and every adjacent pair strictly ordered, checked against file info read directly from disk; skipped with -short), `TestListOrders` (case-insensitive names without case-only duplicates, which Windows cannot store), `TestListPageAndCursor` (**the folder changes between pages**: an already-shown item removed and a new one inserted after the cursor; the next page is [bb c]), `TestListErrors` (9 cases). api: folder response validated against the ItemsResponse schema (paths with /, UTC times), file details, paging with a cursor (desc), **invalid input never reaches the service** (8 bad queries → 400 invalid_request, 0 service calls), no files service → 501; the contract test has 9 files error cases (400 invalid_request ×5, outside_root, invalid_name, 404, 405).
- Test-writing mistakes caught and fixed: `%2F` in a query decodes to `/` (so the path was valid, a 404 not a 400); backslash case used instead. One more heredoc `\n` → newline in a Go string (fixed with Edit; code with backslashes now goes through Write/Edit only). The fake service had an unused field (removed).
- Results: all packages OK on Windows (files ~15 s for the 10,000-entry test); Linux (WSL) files (~3 s) and api PASS; drift check clean; FuzzAPI 45 s PASS; golangci-lint 0 issues (windows + linux); coverage gate passes.

### E069 | 12:25 | USER + TASK START S01.3-T03 (write-ahead)
- User message: "continue".
- S01.3-T02: CI run 35969161697 green (including the 10,000-entry test under `-race` on Linux); merged into `develop` and pushed. Branch `feat/S01.3-T03-details`; T03 In Progress.
- Design (drafted while T02's CI ran):
  - MIME: `mime.TypeByExtension` (lower-cased extension), else `http.DetectContentType` on the first 512 bytes.
  - ETag: strong, `"` + 24 hex of SHA-256(size | mtime ns | file ID) + `"`. A rewrite changes the time, and a replacement by rename changes the file ID.
  - `storage.FileID(*os.File)`: device:inode on Unix (from `Stat_t`), volume serial:file index on Windows (`GetFileInformationByHandle`). It lives in storage because files may not import syscall.
  - Details only for `Stat` (one file). Listings get the extension MIME only: no sniffing and no ETag, which would mean opening every entry.
  - Tests assert only Go's built-in extension table (.pdf, .png, .json), because Linux (/etc/mime.types) and Windows (the registry) add their own entries.

### E070 | 12:28 | TASK DONE S01.3-T03 (item details)
- Code: `internal/storage/fileid_{unix,windows}.go` (+ test), `internal/files/details.go` (mimeByExtension, withDetails, etag); `Local.Stat` returns details; `readFolder` adds the extension MIME to files; spec `FileItem.mime`, `FileItem.etag` (regenerated); `api.fileItem` sets them.
- Tests: `TestStatDetails` (8 fixtures: .pdf/.PNG/.json by extension; a PDF without an extension, plain text, an unknown extension with binary content, and an empty file by sniffing; a folder has no MIME or ETag; the ETag format is a quoted 24-digit hex), **`TestETagFollowsContent`** (stable; changes after a rewrite; **changes after an atomic replacement with the same size and time**, through the file ID), `TestListShowsMIMEByExtensionOnly`, `TestFileID` (two handles to one file agree; two files differ; after a rename the name has the other file's ID; a closed handle → error), api `TestGetItemsFile` (mime present, ETag format) and `TestGetItemsListingHasNoETags`.
- Results: all packages OK on Windows; Linux (WSL) details/FileID/api PASS; drift check clean; golangci-lint 0 issues (windows + linux); coverage 94.9%.

### E071 | 12:32 | TASK START S01.3-T04 (write-ahead)
- S01.3-T03: CI run 35969766390 green; merged into `develop` and pushed. Branch `feat/S01.3-T04-create-folder`; T04 In Progress.
- Design (drafted while T03's CI ran):
  - `files.CreateFolder(owner, path, FolderOptions{Parents, OnConflict})` → (item, created). fail → 409; rename → `name (n)` via a race-safe Mkdir loop (Mkdir is atomic; bounded to 10,000 tries); overwrite → an existing folder is returned unchanged (created=false, HTTP 200), and an existing file → 409 (folders never replace files, as conventions.md says).
  - Name rules apply to the new folder and, with parents, to each missing ancestor. **All new names are checked before any folder is created**, so a refused request creates nothing (the first draft created valid parents before refusing a later name; the test caught it). Existing parents with odd names (made outside the API) are not re-checked.
  - The root "/" → invalid_name/dot_name ("the root folder always exists"). A missing parent without the option → 404; a file as a parent → 409.
  - API `POST /api/v1/files/folders`, JSON `{path, parents, on_conflict}`; 201 created / 200 existing. `strictJSON[T]` runs before the generated decoder: Content-Type must be application/json, unknown fields are refused (conventions.md), there must be one object and no trailing data, and a body over the limit → 413 too_large. The generated decoder is lenient on all of these.

### E072 | 12:37 | TASK DONE S01.3-T04 (create folder)
- Code: `internal/files/folder.go` (OnConflict, FolderOptions, CreateFolder, checkNewName, ensureParent, mkdirWithPolicy); `Service.CreateFolder`; spec `POST /files/folders` with `CreateFolderRequest`, `OnConflict`, and the `Conflict`/`TooLarge` responses (regenerated); `internal/api/body.go` (strictJSON), `internal/api/folders.go`; route `POST /api/v1/files/folders`; review row in `docs/api/conventions.md`.
- Resolver fix found by the tests: on Windows, `filepath.IsLocal` refuses device names (`/con`) before the name rules see them, so the problem had no `rule`. The resolver now reports a reserved segment as invalid_name/reserved_name, the same answer as on Linux (`TestResolveReservedRule`, Windows only).
- Tests: files: `TestCreateFolder` (created; fail → 409; rename → `x (1)`, `x (2)`; overwrite → existing folder unchanged, a file → 409; missing parent → 404; a file parent → 409; the root → invalid_name), `TestCreateFolderParents`, `TestCreateFolderNameRules` (**a refused request creates nothing**, including valid parents before a refused name; a Linux-only existing parent with an odd name is not re-checked), `TestCreateFolderRenameRace` (10 goroutines, 10 distinct folders), `TestCreateFolderHook`. api: `TestCreateFolderEndpoint` (201/200, rename, parents, bodies validated against FileItem, the new folder listed), `TestCreateFolderInvalidInputNeverReachesService` (7 bad bodies + a text/plain body → 400, 0 service calls); the contract test has a new `jsonErrorCases` table (9 cases: 400 invalid_request ×4, invalid_name, outside_root, 404, 409, 413) that counts for route coverage, and a 405 case.
- Results: all packages OK on Windows; Linux (WSL) files, storage, and api PASS; drift check clean; golangci-lint 0 issues (windows + linux); coverage 94.1%.

### E073 | 12:42 | TASK START S01.3-T05 (write-ahead)
- S01.3-T04: CI run 35970636485 green; merged into `develop` (b744584) and pushed. Branch `feat/S01.3-T05-upload`; T05 In Progress.
- Design:
  - `PUT /api/v1/files/content?path=&on_conflict=`, raw body (any Content-Type; the spec says `application/octet-stream`). 201 + FileItem when a new file was created (also with `rename`), 200 when `overwrite` replaced a file.
  - **Declared size first.** `Content-Length` is required: without it (chunked) the answer is **411 with a new code `length_required`** (codes are only ever added; errors.md, the spec enum, and apperr change together). Over `uploads.max_file_size` → 413 too_large; over the free-space reserve → 507 (SpaceGuard). All three come before any byte is read or stored, so a client using `Expect: 100-continue` never sends the body.
  - **Body limits per route.** The global 1 MiB `MaxBytesHandler` would cap uploads, so `Route` gets a `MaxBody` (0 = the default 1 MiB) and `New` wraps each route. The upload route's limit is `uploads.max_file_size`.
  - **Atomic write** (`files.Upload`): a temp file `.local-ai-nas-tmp-<random>.part` in the target folder (same file system, inside the os.Root); copy with a 1 MiB buffer; exactly the declared bytes (short or long body → 400, temp removed); fsync; then commit:
    - `fail` / `rename`: `os.Root.Link(temp, target)`, which fails if the target exists (atomic no-replace; a race-safe `name (n).ext` loop for rename), then remove the temp. On a file system without hard links (FAT/exFAT), fall back to check-then-rename (a small race, documented).
    - `overwrite`: `os.Root.Rename` over a file (atomic replace); a folder → 409.
    - Fail fast: with `fail`, an existing target → 409 before reading the body; `overwrite` onto a folder likewise.
    - Then sync the folder (Unix; best effort).
  - **No partial file is visible:** listings skip the temp prefix, `Stat` of a temp name is not found, and the name rules refuse new names with the prefix (`reserved_name`), so no client can create or address one.
  - Stale temp files after a crash stay hidden. Removing them joins the abandoned-upload cleanup (S01.4-T06), to be recorded in the S01 change log.
  - `files.NewLocal(r, Options{Hooks, Space})`: the service owns the free-space guard (the API cannot see the disk). The rename pattern for files keeps the extension (`a (1).txt`; a dot file `.env (1)`), as conventions.md says.

### E074 | 12:51 | TASK DONE S01.3-T05 (simple streamed upload)
- Code: `internal/files/upload.go` (UploadOptions, Upload, checkTarget, writeTemp, copyExactly, commitFile, numberedName, placeNew with `linkFile`, syncFolder); `files.Options{Hooks, Space}` replaces the hooks argument of `NewLocal`; `resolveVisible` hides temporary names from Stat and List, and `readFolder` skips them; `storage.TempPrefix`/`IsTempName` plus a `reserved_name` rule for the prefix; `apperr.LengthRequired` (411 `length_required`); spec `PUT /files/content` (`uploadFile`) with the LengthRequired and InsufficientStorage responses (regenerated); `internal/api/content.go` (declaredSize, UploadFile, conflictPolicy, shared with CreateFolder); per-route body limits (`Route.MaxBody`, `Options.MaxUploadBytes`); `serve` passes the SpaceGuard and `uploads.max_file_size`.
- Docs: conventions.md "Simple upload" section and review row; errors.md code `length_required` and the reserved prefix; config comments and the example config (max_chunk_size is tus only); S01 change log row (design details, S01.4-T05 part done early, a cleanup note for S01.4-T06).
- Tests: files: **`TestUploadIsByteIdentical`** (0 B to 3 MiB + 17, around the 1 MiB buffer; SHA-256 equal; no temporary file left), `TestUploadConflictPolicies` (13 cases: fail, rename `a (1).txt`/`.env (1)`/`x.tar (1).gz`, overwrite replaces a file and never a folder), **`TestUploadFailuresLeaveNothing`** (13 cases: short, long, read error, cancelled, 507, 404, 409, name rules, outside root; the disk is unchanged, and **refusals before the body read 0 bytes**), **`TestUploadInvisibleWhileWriting`** (during an upload: the listing shows neither name, Stat and List of both → 404; then the complete file appears), `TestUploadRenameRace` (10 concurrent writers, 10 names, each with its own content), `TestUploadWithoutHardLinks` (fallback) and `TestUploadRaceWithHardLinks` (a name taken during the upload → 409, the first writer's file kept), `TestUploadHook`, `TestNumberedName`, the temp-prefix name rules. api: `TestUploadEndpoint` (3 MiB body over the 1 MiB JSON limit, rename, overwrite 200, empty, Unicode name; FileItem schema; SHA-256 on disk), `TestUploadInvalidInputNeverReachesService`, **`TestUploadOverHTTP`** (real server and client: 413 and 409 **before any body byte with `Expect: 100-continue`**, chunked → 411, 4 MiB streamed with and without Expect); the contract test's `bodyErrorCases` (generalized from jsonErrorCases) has 14 upload error cases.
- Results: all packages OK on Windows; Linux (WSL; ext4 temp dirs, hard links) files, storage, api, apperr PASS; FuzzAPI 40 s (1.42 M execs) PASS; drift check clean; golangci-lint 0 issues (windows + linux); coverage 93.5%.

### E075 | 12:53 | PLAN S01.3-T06 (write-ahead, drafted while T05's CI runs)
- S01.3-T05 pushed (9ca6200); CI running. T06 work starts in the working tree and moves to `feat/S01.3-T06-download` after T05 is merged (git stash across the merge).
- Design:
  - `files.Download(owner, path) (Item, io.ReadSeekCloser)`: Lstat → only a regular file (a folder or link → 400 invalid_request; links are never followed); open; then fstat and `os.SameFile` with the Lstat result, so a file swapped in between is refused (409). The details (ETag, MIME) come from the open handle, so the ETag belongs to the bytes served. Temporary names → 404 (resolveVisible). Hook OpDownload.
  - API `GET /api/v1/files/content?path=` (HEAD too, through the GET pattern): the operation returns a custom response object whose visitor calls `http.ServeContent` with the request (a route wrapper puts it in the context). ServeContent handles Range (single, open-ended, suffix, multi-range), If-None-Match/If-Modified-Since (304), If-Range, If-Match/If-Unmodified-Since (412), and 416.
  - Every error stays a problem: a status writer turns ServeContent's plain-text 412 and 416 into problems with **two new codes, `precondition_failed` (412) and `range_not_satisfiable` (416)**, and keeps `Content-Range: bytes */size`.
  - Headers: Content-Type from the item's MIME (never sniffed again), ETag, Last-Modified, `Cache-Control: private, no-cache` (revalidate with the ETag instead of no-store), `X-Content-Type-Options: nosniff`, `Content-Security-Policy: sandbox` (an uploaded HTML file cannot run as the app's origin), and `Content-Disposition: attachment; filename="<ASCII fallback>"; filename*=UTF-8''<RFC 8187>` (RFC 6266). Inline display is left to S02 (GUI previews), with this CSP.

### E076 | 12:56 | TASK START S01.3-T06 (download)
- S01.3-T05: CI run 35971985758 green; merged into `develop` (8f4fba6) and pushed. Branch `feat/S01.3-T06-download` (the draft from E075 moved over with git stash); T06 In Progress. Design as in E075.

### E077 | 12:59 | TASK DONE S01.3-T06 (download)
- Code: `internal/files/download.go` (Download, openFile: Lstat → regular files only → open → `os.SameFile`), `fileDetails` (details from an open handle with ReadAt, shared with withDetails); `Service.Download`; apperr kinds `PreconditionFailed` (412) and `RangeNotSatisfiable` (416); spec `GET /files/content` (`downloadFile`) with the PreconditionFailed and RangeNotSatisfiable responses (regenerated); `internal/api/download.go` (withRequest, DownloadFile, fileContent visitor around `http.ServeContent`, problemStatus, attachment, encodeExtValue); `server.log`.
- Docs: conventions.md "Download" section and review row; errors.md codes; S01 change log row (new codes, attachment + CSP, a note for S02 inline previews, a note for S01.7-T04: no sendfile through the middleware writers).
- Tests: files `TestDownload` (details equal Stat's, sniffed MIME, the sniff does not move the read position, Seek), **`TestDownloadServesTheVersionItDescribes`** (a file replaced during a download: the open download still sends the old bytes, and the next one has a new ETag; this also works on Windows), `TestDownloadRefuses` (folder, root, missing, a file as a parent, a temporary name, outside root, and a symbolic link on Linux), `TestDownloadHook`. api **`TestDownloadEndpoint`** (headers; single, open-ended, suffix, past-the-end, unsatisfiable, malformed, and multiple ranges; If-None-Match, If-Modified-Since, If-Range matching and stale, If-Match, If-Unmodified-Since, HEAD; 412 and 416 validated as problems without download headers), `TestDownloadErrors`, **`TestContentDisposition`** (7 names; `mime.ParseMediaType` returns each exact name), `TestDownloadUnicodeName`; 4 contract cases.
- Results: all packages OK on Windows; Linux (WSL) files, api, apperr PASS; FuzzAPI 30 s PASS; drift check clean; golangci-lint 0 issues (windows + linux); coverage 93.3%.

### E078 | 13:00 | PLAN S01.3-T07 (write-ahead, drafted while T06's CI runs)
- Endpoints (stage doc 4.3): `POST /api/v1/files/operations/rename` `{path, new_name, on_conflict}` (same folder; `new_name` is one name, checked by the name rules) and `POST /api/v1/files/operations/move` `{from, to, on_conflict}` (`to` is the full new path; its parent must exist). Both answer 200 with the moved item. Service: `Rename` and `Move` share one implementation, with hooks OpRename/OpMove and `Event.Target`.
- Checks before anything moves: the source exists and is not the root (temporary names are not found); the target's name rules and path length; the target's parent exists and is a folder. **A folder cannot move into itself or below itself:** each ancestor of the target is compared with the source using `os.SameFile`, which also catches case variants on Windows (`/Docs` → `/docs/sub`), where a string prefix check would miss.
- Same item: a move onto the source itself (the same path, or a case-only change on a case-insensitive disk) is a plain rename, or a no-op when the path is identical.
- Commit per policy:
  - Files with `fail`/`rename` use `placeNew` (hard link, then remove the source), an atomic no-replace move, as in T05. Folders and links use a check followed by a rename (no portable no-replace rename for folders through os.Root; a small race, documented).
  - `overwrite` replaces a file with a file atomically (rename). A folder is never replaced or merged (409), and a folder never replaces a file (409).

### E079 | 16:12 | USER + TASK START S01.3-T07 (rename and move)
- User message: "continue".
- S01.3-T06: CI run 35972700242 green; merged into `develop` (9b5fbf3) and pushed. Branch `feat/S01.3-T07-rename-move`; T07 In Progress. Design as in E078.

### E080 | 16:19 | TASK DONE S01.3-T07 (rename and move)
- Code: `internal/files/move.go` (MoveOptions, Rename, Move, move, refuseIntoItself, moveWithPolicy, numbered, moveNew); `placeNew` generalized for moves (removes the new link again if removing the source fails); `Service.Rename`/`Move`; spec `POST /files/operations/rename` (`renameItem`, RenameRequest) and `POST /files/operations/move` (`moveItem`, MoveRequest) (regenerated); `internal/api/operations.go`; routes with strictJSON.
- Docs: conventions.md "Rename and move" section and two review rows; S01 change log row (endpoint bodies; folders move with check + rename, a question for S01.6-T04).
- Tests: files `TestRename` (file, folder with contents, **case-only rename, also on Windows**, same name), `TestMove` (file, rename by move, folder with contents, `/docs` → `/docs2/docs` where the names only share a prefix), `TestMoveConflictPolicies` (10 cases: fail, rename for files `t (1).txt` and folders `target (1)`, overwrite of a file, and a folder never replacing or replaced), **`TestMoveIntoItself`** (into itself, below itself, and through `/DOCS` on Windows; onto itself is nothing to do), `TestMoveErrorsLeaveNothing` (11 move and 7 rename refusals; disk unchanged), **`TestMoveConcurrentToOneName`** (10 concurrent moves: with fail exactly one wins, with rename all succeed; no file lost), `TestMoveWithoutHardLinks`, `TestMoveHooks` (OpRename/OpMove with Target). api `TestRenameMoveEndpoints` (5 operations, FileItem schema, listings after), `TestOperationsInvalidInputNeverReachesService` (14 bad bodies, 0 service calls); 19 contract cases.
- Results: all packages OK on Windows; Linux (WSL) files and api PASS; FuzzAPI 30 s PASS; drift check clean; golangci-lint 0 issues (windows + linux); coverage 92.8%.

### E081 | 16:20 | PLAN S01.3-T08 (write-ahead, drafted while T07's CI runs)
- `POST /api/v1/files/operations/copy` `{from, to, on_conflict}` → 201 with the copy. Service `Copy` (hook OpCopy with Target).
- **Pre-scan before any write:** walk the source without following links. It counts items and bytes against new settings `copy.sync_max_items` (default 1000) and `copy.sync_max_bytes` (default 1 GiB); over either, the answer is **a new code `too_large_for_sync` with status 422** (the request is valid but cannot be done synchronously; the job-based copy comes in S04.3). The plan does not name a status; 413 means the request content is too large, which does not fit. The scan also checks every name that will be created against the name rules and the path length, and refuses links and special files in the tree (links are never followed, and a silent skip would make the copy incomplete). Temporary names are skipped. Then the free-space guard gets the byte total (507).
- A folder cannot be copied into itself or below itself (the `os.SameFile` ancestor check from T07).
- **Nothing partial is visible:**
  - A file is copied into a temporary file in the target folder (1 MiB buffer, fsync), then committed like an upload: Link for fail/rename, Rename for overwrite.
  - A folder is built completely under a temporary name in the target's parent (every file fsynced), then renamed to its name in one step; on any error or cancellation the temporary tree is removed.
  - Modification times of files and folders are kept.
- Conflicts at the top level: `fail` → 409 before copying; `rename` → `name (n)`; `overwrite` → a file replaces a file; folders are never merged or replaced.

### E082 | 16:25 | TASK START S01.3-T08 (copy) + a race fix for T07
- S01.3-T07: CI run 35992364545 green; merged into `develop` (373277e) and pushed. Branch `feat/S01.3-T08-copy` (the draft from E081 moved over with git stash); T08 In Progress. Design as in E081.
- **Race found by the new `TestCopyConcurrentRename` on Windows:** two folder renames to one free name: both checks pass, the second rename fails with "Access is denied" (not "exists"), so it became a 500 instead of the next `name (n)`. The same path serves folder moves from T07. Fix: `renameIfFree` (shared by the no-hard-link fallback of `placeNew` and by `moveNew`) re-checks the target after a failed rename and reports a name that exists by then as taken. New `TestMoveConcurrentFolders` covers T07's path; both pass 3 runs in a row.

### E083 | 16:28 | TASK DONE S01.3-T08 (copy)
- Code: `internal/files/copy.go` (CopyLimits, CopyOptions, Copy, scanCopy, readNames, checkCopiedName, copyOneFile, copyFolder, buildTree, commitFolder, copyFileTo, copyStream); `renameIfFree` in upload.go (used by placeNew's fallback and moveNew); `refuseIntoItself` takes the verb; `files.Options.CopyLimits`; apperr `TooLargeForSync` (422); config `copy.sync_max_items`/`copy.sync_max_bytes` (validation, example config, tests); `serve` passes the limits; spec `POST /files/operations/copy` (`copyItem`, CopyRequest, TooLargeForSync response) (regenerated); `api.CopyItem` and route.
- Docs: conventions.md "Copy" section and review row; errors.md `too_large_for_sync`; S01 change log row (422, settings, temp tree, links refused, times kept, the race fix).
- Tests: files **`TestCopyTreeIsByteIdentical`** (nested tree with files of 0 B to 2 MiB, an empty folder; every file equal, the source unchanged, **modification times of files and folders kept**; no temporary files), `TestCopyConflictPolicies` (11 cases, including a copy next to the original with rename), **`TestCopyRefusalsLeaveNothing`** (13 cases: into itself, root, onto root, missing, parent, reserved, temporary source, **too many items, too many bytes (422)**, no free space, cancelled folder and file; disk unchanged each time), `TestCopyLimitsAreInclusive`, `TestCopyTreeRules` (temporary files not copied; on Linux a rule-breaking name → invalid_name and a link → 400, nothing left), **`TestCopyConcurrentRename`** (6 concurrent folder copies, all complete and distinct), `TestCopyHook`, `TestMoveConcurrentFolders`. api `TestCopyEndpoint` (201/200, listings, content after overwrite), `TestCopyTooLargeForSync` (422 problem against the schema), `TestCopyInvalidInputNeverReachesService`; 9 contract cases.
- Results: all packages OK on Windows; Linux (WSL) files, api, config PASS; FuzzAPI 30 s PASS; drift check clean; golangci-lint 0 issues (windows + linux); coverage 91.8%.

### E084 | 16:29 | PLAN S01.3-T09 and the S01.3 closure (write-ahead, drafted while T08's CI runs)
- T09: `DELETE /api/v1/files/items?path=&recursive=` → 204. Service `Delete(owner, path, DeleteOptions{Recursive})`, hook OpDelete.
  - The root → 400. A file or link → `os.Root.Remove` (a link is removed itself, never its target).
  - A folder without `recursive` → Remove, and a non-empty folder → 409. When the only entries are hidden temporary files, the message says an upload or copy is unfinished.
  - With `recursive` → `os.Root.RemoveAll` (confined to the root, links not followed). It is not atomic: a failure part-way leaves the rest, and a retry finishes it. Renaming to a temporary name first would hide that, but on Windows a folder with an open file inside cannot be renamed, so the delete would fail more often. Permanent in S01; trash comes in S08.
- S01.3 closure (plan S01.3 acceptance): criterion 1 "every operation works over HTTP against a real temporary filesystem" gets an end-to-end test through a real server and client (create folder, upload, list, details, ranged download, rename, move, copy, delete). Criterion 4 "no operation can read or write outside the namespace" gets a test that aims every endpoint at escapes (`..`, encoded separators, backslashes, drive letters, UNC, absolute paths). Sentinel files in the photos area, a second namespace, and the internal data must stay unread and unchanged.

### E085 | 16:37 | TASK DONE S01.3-T09 (delete) + S01.3 CLOSED
- S01.3-T08: CI run 35993223778 green; merged into `develop` (cb36f5c) and pushed. Branch `feat/S01.3-T09-delete`.
- Code: `internal/files/delete.go` (DeleteOptions, Delete, notEmpty); `Service.Delete`; spec `DELETE /files/items` (`deleteItem`, `recursive`) (regenerated); `api.DeleteItem` and route; **`storage.IsEscape`** (os.Root's unexported "path escapes from parent" error, matched by message with a test that fails if the text changes) mapped to `outside_root` in `fsError`.
- **Found by the closure test on Linux:** every operation on a path through a symbolic link to the outside (`/escape-abs/secret.jpg`) was refused by os.Root, but answered **500**; now 400 outside_root. A test mistake was fixed too: operations on the link items themselves (list, rename, delete the link) are legitimate namespace operations; they now only have to avoid 5xx and leaks.
- Tests: files `TestDelete` (file, empty folder, full folder refused then recursive, root, missing, through a file, outside), `TestDeleteFolderWithUnfinishedWrite`, `TestDeleteLinkNotTarget` (Linux), `TestDeleteHook`; storage `TestIsEscape`; api **`TestFileOperationsOverHTTP`** (all 9 operations through a real server and client), **`TestNoOperationLeavesTheNamespace`** (10 operations × 18 escape paths, + 5 through links and 3 link items on Linux, + 5 bad new names; all 4xx, no secret returned, a snapshot of everything outside the namespace unchanged, the file outside the storage root unchanged), `TestDeleteInvalidInputNeverReachesService`; 6 contract cases.
- Docs: conventions.md "Delete" section and review row (every S01.3 endpoint now has a row); S01 change log rows (T09 details; the S01.3 closure with all 5 criteria); plan.md S01.3 status Done; the stage's substage table.
- Results: all packages OK on Windows; Linux (WSL) files, api, storage PASS; FuzzAPI 30 s PASS; drift check clean; golangci-lint 0 issues (windows + linux); coverage 91.7%.

### E086 | 16:38 | PLAN S01.6-T03 symlink policy (write-ahead, drafted while T09's CI runs)
- Today: a link as the *last* element is never followed. Stat and List use Lstat and show it as `kind=symlink` without a target; download and copy refuse it; an overwrite onto it is 409; rename, move, and delete act on the link itself. os.Root refuses links that leave the area (now 400, E085). **Gap:** os.Root still *follows* a link inside the area when it is a middle element (`/in-link/a.txt` where `in-link` → `docs`), for reads and writes.
- Change: every operation checks that no parent element of each path it uses (source and target) is a link or another non-folder special entry (Windows junctions report `ModeIrregular`). Such a path gets **400 invalid_request** "… goes through the symbolic link …; links are not followed". Missing elements stop the check, so 404 and parent handling stay as they are. One Lstat per element and path. A link swapped in after the check is still confined by os.Root.
- The API never creates links: an architecture test checks that the non-test files of `internal/files` never call a `Symlink` function.
- Tests: links inside the area (to a folder and to a file) and outside (absolute and relative). Every operation through a link as a middle element → 400. Operations on the link item: listed without a target; download, copy, and overwrite refused; rename, move, and delete act on the link and never touch the target.
- Link creation moves into `testutil.Symlink`: it skips with the reason when the OS refuses (Windows without the privilege), so the tests also run on the Windows CI runner, where links are allowed (the acceptance asks for this). The earlier `GOOS == windows` skips move to the helper too.

### E087 | 16:42 | TASK START S01.6-T03 (symlink policy)
- S01.3-T09 + S01.3 closure: CI run 35994100122 green; merged into `develop` (feefddd) and pushed. **S01.3 is Done.**
- Branch `feat/S01.6-T03-symlinks` (the draft from E086 moved over with git stash); T03 In Progress. Design as in E086; drafted so far: `files/links.go` (`refuseLinkParents`, called by all 10 operations for every path), `TestNoLinksCreated` + `TestSymlinkCallsDetected`, `testutil.Symlink`/`TrySymlink` (the old `GOOS == windows` skips of link tests replaced), `links_test.go`.

### E088 | 16:45 | TASK DONE S01.6-T03 (symlink policy)
- Code: `internal/files/links.go` (`refuseLinkParents`), called by Stat, List, CreateFolder, Upload, Download, Rename/Move (source and target), Copy (source and target), and Delete; `testutil.Symlink` and `TrySymlink` (+ tests); the link tests in files, storage, and api use them instead of skipping on Windows.
- Docs: conventions.md "Symbolic links" section; S01 change log row.
- Tests: **`TestLinksAreNeverFollowed`** (11 operations × 7 paths through links: to a folder and a file inside the area, to a folder outside the storage root, and relatively to the photos area; every one 400 about the link; targets unchanged). **Mutation check:** with the check disabled, all 77 cases fail on Linux (os.Root would follow the inside links, and the outside ones would be outside_root without the link message), so the test guards the policy. `TestLinkItems` (listed as symlink without size, MIME, or ETag; download, list, and copy of a link refused; overwrite and folder-over-link 409; rename and delete act on the link only). `TestNoLinksCreated` + `TestSymlinkCallsDetected` (the check finds `root.Symlink` and `os.Symlink`).
- Results: all packages OK on Windows (the link tests skip there locally: no symlink privilege); Linux (WSL) files, api, storage, testutil PASS with every link test running; drift check clean; golangci-lint 0 issues (windows + linux); coverage 91.1%.

### E089 | 16:46 | PLAN S01.6-T04 and T05 (write-ahead, drafted while T03's CI runs)
- **T04 (conflict handling):** the policies already exist for upload, create folder, copy, move, and rename (S01.3). T04 adds the acceptance evidence:
  - `TestConflictMatrix`: every operation (upload, create folder, copy of a file and of a folder, move of a file and of a folder, rename of a file and of a folder) × every policy (fail, rename, overwrite) × every target state (free, a file, a folder), with the expected result, name, and created flag written out in one table.
  - `TestConcurrentRenameCollisions`: for every operation, 8 concurrent calls to one name with `rename` give 8 distinct complete items.
  - **tus finalize** is not built yet; S01.4-T03 will commit through the same functions (commitFile) and add its rows to the matrix. Recorded in the change log.
- **T05 (concurrency):**
  - `storage.Locks`, a keyed mutex with reference counting; keys are case-folded, so "A" and "a" share a lock where the disk ignores case.
  - The files service locks the *target folder* only for the commit step. The long part (writing a temporary file or tree) stays outside the lock. That makes the check-then-rename of folders and the no-hard-link fallback race-free within the server, which is the only writer to the area.
  - Stress test: 50 concurrent writers to one name with each policy give one consistent final file (exactly one writer's bytes), no partial or temporary files, and no reports from `-race` (the Linux CI job runs it).

### E090 | 16:50 | TASK DONE S01.6-T04 (conflict handling)
- S01.6-T03: CI run 35994898119 green (its Windows job now creates links where the runner allows); merged into `develop` (9e34589) and pushed. Branch `feat/S01.6-T04-conflicts`.
- Tests: **`TestConflictMatrix`**: upload, create folder, copy of a file and of a folder, move of a file and of a folder, rename of a file and of a folder × fail, rename, overwrite × a free name, an existing file, an existing folder = **72 cases**. Each checks the error or the name used and the created flag, the new content, and that the existing item survives unless it was overwritten. **`TestConcurrentRenameCollisions`**: 8 concurrent calls to one name with `rename` for each of the 8 operations, all distinct.
- Docs: conventions.md conflict table (folders numbered whole, concurrent requests, overwrite of a folder by a new folder returns it); S01 change log row; the tus finalize rows join with S01.4-T03 (CURRENT_STATE next steps).
- Results: files tests PASS on Windows with only the T04 files on top of develop (the T05 draft stashed while testing).

### E091 | 16:55 | TASK DONE S01.6-T05 (concurrency safety)
- S01.6-T04: CI run 35995474430 green; merged into `develop` (ee3a95f) and pushed. Branch `feat/S01.6-T05-locks` (the T05 draft from E089 committed alone; the T06 draft stays in the working tree).
- Code: `internal/storage/locks.go` (`Locks`: keyed mutexes, reference-counted, case-folded keys); `Local.lockFolder` around the commit steps of upload, create folder, copy (file and folder), and move/rename; `openFile` looks a replaced file up again (up to 5 tries) before 409.
- Tests: storage `TestLocksExclude` (50 holders of one key in two spellings never overlap; other keys do not wait; keys are dropped). files **`TestStressConcurrentWriters`**: 50 concurrent writers (64 KiB each) to one name with overwrite, fail, and rename, while 4 readers keep downloading it. Results:
  - overwrite: all 50 succeed and **exactly one reports it created the file** (the lock makes that exact);
  - fail: exactly one succeeds and the file is its bytes;
  - rename: 50 distinct files, each with its writer's bytes;
  - every read is one writer's complete file;
  - no temporary files are left.
  The first run showed readers getting 409 "changed while it was opened" under constant overwrites (the lookup guard from T06 working as designed but too eagerly), which led to the retry.
- Results: all packages OK on Windows (stress ~8 s) and Linux (WSL, 0.08 s); golangci-lint 0 issues (windows + linux). `-race` runs in the Linux CI job (the race detector needs cgo, which the local toolchain does not have).

### E092 | 17:02 | TASK START S01.6-T06 (bind guard) + PLAN (recorded late) + S01.6 closure gap
- S01.6-T05: CI run 35995930046 green (the stress test ran under `-race` on Linux); merged into `develop` (6360222) and pushed. Branch `feat/S01.6-T06-bind-guard`.
- **Process note (R3):** the T06 draft below was written while T05's CI ran, before this plan entry. The plan was only in my working notes, not in this log. Recorded now, late.
- T06 design:
  - `server.bind` must be a loopback address: `localhost`, a loopback IP (127.0.0.0/8, ::1), or a host name whose every address is loopback (resolved with a 3 s timeout; an unresolvable name is refused). Otherwise config validation fails with a message that points to stage S03.
  - The **container exception** (the S01 change-log note from S01.1-T06): a new boolean setting `server.allow_container_bind` is honoured only inside a container (`/.dockerenv` or `/run/.containerenv`). The dev compose file and the CI container job set it; outside a container it is refused with an explanation.
  - A boolean setting kind for the config (TOML booleans; `true`/`false` from environment and flags).
- **S01.6 closure gap found while checking the criteria:** plan S01.6 criterion 1 lists "Unicode look-alikes" in the attack corpus, but the corpus had none. Fix:
  - The resolver refuses segments whose NFKC form is a dot name or a dot run (`．．`, `‥`, `…`).
  - The name rules refuse separator look-alikes (`／ ＼ ∕ ⁄ ∖ ⧵ ⧸ ⧹ ﹨`, new rule `lookalike_separator`) and dot look-alike names (`dot_name`).
  - Other fullwidth characters stay allowed, because characters such as `？` are common in CJK names.
  - 6 look-alike paths join the attack corpus.

### E093 | 17:05 | TASK DONE S01.6-T06 (bind guard) + S01.6 CLOSED
- Code: config `Server.AllowContainerBind`, `boolSetting` (kind `kindBool`; TOML booleans, `true`/`false` elsewhere), `checkLoopback` (with a replaceable `lookupHost`), `inContainer`; the bind check in `validate`; `deploy/config.example.toml`, `deploy/compose.dev.yaml`, and the CI container job set or document the setting; README note. Storage: `isLookalikeDots` in the resolver; `RuleLookalikeSep` and the dot look-alike check in `ValidateName`; 6 look-alike paths in the attack corpus; errors.md name rules.
- Tests: **`TestBindGuard`** (accepted: 127.0.0.1, 127.3.4.5, [::1], localhost in two cases, a name resolving only to loopback; refused with a message naming S03: 0.0.0.0, [::], two LAN IPs, a link-local IPv6, a name resolving to a LAN IP, a name with a loopback and a LAN address, an unresolvable name), `TestContainerBind` (only inside a container and only with the setting), `TestBoolSetting` + TOML type cases; name rule cases for look-alikes (11); FuzzValidateName and FuzzResolve 30 s each PASS.
- Docs: S01 change log rows (T06 details, the look-alike gap, **S01.6 closed** with all 5 criteria); plan.md S01.6 status Done; the substage table.
- Results: all packages OK on Windows; Linux (WSL) storage, config, api, files PASS; drift check clean; golangci-lint 0 issues (windows + linux); coverage 91.4%.

### E094 | 17:07 | PLAN S01.4 overview and S01.4-T01 (write-ahead, drafted while T06's CI runs)
- **S01.4 shape (ADR-0008):** `internal/uploads` embeds tusd v2.10.1 (`pkg/handler`, `pkg/filestore`, `pkg/filelocker`), with the store at `<internal>/tmp/uploads/` and the route `/api/v1/files/uploads/` (POST, HEAD, PATCH, DELETE, OPTIONS; no GET downloads from the tus store). The `uploads` table indexes the sessions.
- **Finalize (T03):** the tus data file sits outside the namespace's os.Root, so os.Root cannot rename it.
  - `internal/storage` (the layer allowed to use paths) moves the finished file into the target folder under a hidden temporary name (`.local-ai-nas-tmp-*`).
  - The files service then commits it with the existing policy code (lock, Link/Rename, conflict policy).
  - The cross-root move uses absolute paths after the os.Root checks (no links among the parents). Its small window matters only to a writer other than the server, which is outside the threat model; to be recorded as a design note.
  - When the health check reported different file systems, finalize streams the data file through the existing `files.Upload` (copy + fsync + commit).
- **T01 (this task):**
  - `go get github.com/tus/tusd/v2@v2.10.1`; register it in `dependencies.md` in the same commit (R6); licenses and govulncheck must pass for every linked module.
  - `uploads.New(Options{Dir, Index, Logger, MaxSize, BasePath})` returns the tusd handler with creation, creation-with-upload, and termination, and download and concatenation disabled.
  - Its creation hook records the session (id, namespace, target path from metadata, policy, size, expiry) in SQLite; its termination hook removes the row.
  - The API mounts it at `/api/v1/files/uploads/` with its own body limit (`uploads.max_chunk_size`), and the spec documents it as an external protocol (conventions.md already describes the metadata).
  - T01 validates only what it needs to store a row; the full validation (name rules, sizes, free space) is T02, and finalize is T03.
- T01 test: an in-test tus client (plain net/http) creates an upload, sends part of it, is interrupted, asks HEAD for the offset, resumes, and completes. The stored data equals the source (SHA-256), and the session row exists.

### E095 | 17:10 | TASK START S01.4-T01 (embed tusd)
- S01.6-T06 + S01.6 closure: CI run 35996941498 green (the image job exercised the container exception); merged into `develop` (f22d0e4) and pushed. **S01.6 is Done.**
- Branch `feat/S01.4-T01-tusd` (the draft from E094 moved over with git stash); S01.4 In Progress, T01 In Progress. Design as in E094. The drafted `internal/uploads` (index.go, uploads.go) compiles once `go.sum` has tusd's module requirements (next).

### E096 | 17:28 | TASK DONE S01.4-T01 (embed tusd)
- Dependency (R6): `github.com/tus/tusd/v2 v2.10.1` and `golang.org/x/exp` (direct, for the logger adapter) in `go.mod`; tusd links github.com/tus/lockfile v1.2.0 (MIT) too. Register updated (tusd row; a new x/exp row). `scripts/check-licenses.sh` exit 0; govulncheck: no vulnerabilities. No new deployment prerequisite (section 12 unchanged).
- Code:
  - `internal/uploads`: `uploads.go` (Options, New, beforeCreate, beforeTerminate, refuse), `index.go` (Session, Index: Add/Get/Remove on the `uploads` table), `problems.go` (tusd errors → problems with tusd's status), and `tuslog.go` (x/exp/slog → log/slog adapter, warnings and errors only).
  - apperr gains `Locked` (423) and `Unavailable` (503).
  - API: `Options.Uploads` and `MaxChunkBytes`, `UploadsPath`, two catch-all routes with the chunk body limit, and 501 when no tus server is set.
  - Spec: an `uploads` tag documenting tus (excluded from generation and from the spec-to-route test).
  - `serve` wires it up.
  - Resolver: on Windows, refuse segments ending with a dot or a space.
- Mistake caught: the first resume test deadlocked. After cancelling, the HTTP transport waited for its body writer, which blocked on the test's pipe; the test now closes the pipe right after cancelling (a test bug, not a server one).
- Tests: **`TestResumeInterruptedUpload`** (3 MiB: create; send 1 MiB, then the connection is cut; HEAD reports the stored offset; resume from it; **the stored data's SHA-256 equals the source**; the session row has target, policy, size, owner, and expiry; 3 runs in a row), `TestCreateRefusals` (5 metadata refusals as problems, 400/411, nothing stored), `TestTerminate` (data and session removed), **`TestProtocolErrorsAreProblems`** (412, 404, 409, 400, 400, 405 with tusd's status and headers). `TestTusLogger` (warnings pass with attributes and groups; info is dropped). FuzzAPI now runs with a real tus server: 60 s, 1.65 M execs, PASS, after the fix below.
- **FuzzAPI finding:** `PUT /api/v1/files/content?path=/ /0` answered 500 on Windows, because the segment " " names the folder itself there (`os.Root.Lstat(" ")` succeeds, and "docs." aliases "docs"). Fix in the resolver (Windows only); the Windows corpus gains 5 such paths; the failing input stays as a fuzz seed in `internal/api/testdata/fuzz/FuzzAPI/`.
- Docs: conventions.md tus section (extensions, limits, errors, expiry); errors.md `locked` and `unavailable`; S01 change log row.

### E097 | 17:29 | PLAN S01.4-T02 (write-ahead, drafted while T01's CI runs)
- `files.Local.CheckUpload(owner, path, size, UploadOptions)` runs every check the simple upload makes before it reads a body: resolver, name rules, links among the parents, parent folder, the conflict policy against the current target (fail → 409 early; overwrite onto a folder → 409), and free space for the size. `Upload` uses the same function, so the two cannot drift.
- `internal/uploads` gets an interface of its own, `Target` (`CheckUpload`; T03 adds the finalize), which `files.Local` implements. The API's `files.Service` stays as it is. `beforeCreate` calls it after its own metadata checks; a refusal is a problem with its status (400, 404, 409, 507) and happens before tusd stores anything, so no `.bin`/`.info` file and no session row remain.
- `Upload-Length` over `uploads.max_file_size` is refused by tusd itself (413, via `MaxSize`) before the hook; it becomes a problem through `problemBodies`.
- Tests: every refusal (outside root, invalid names including the temp prefix, a link among the parents, missing parent, file as parent, existing target with fail, a folder with overwrite, over the size limit, no free space) → the problem, an empty upload directory, and no session row.

### E098 | 17:33 | TASK DONE S01.4-T02 (metadata validation)
- S01.4-T01: CI run 35999368786 green; merged into `develop` (0e6a674) and pushed. Branch `feat/S01.4-T02-validation`.
- Code: `files.Local.CheckUpload` with `prepareUpload` and `checkUploadTarget`, which `Upload` now uses too (one set of checks); `uploads.Target` (the narrow interface; `files.Local` implements it) and `Options.Files`; `beforeCreate` calls it after its own metadata checks; `serve` builds one files service for the API and the tus server.
- Tests: **`TestCreateChecksTarget`** (11 cases, each a problem with its status, an empty upload directory, and no session row: outside root, an encoded separator, a reserved name, the temp prefix, the root, a missing parent, a file as parent, an existing target with fail, overwrite onto a folder, over `max_file_size` (413 from tusd, converted), no free space (507); plus a link among the parents on Linux); the tus tests now run against a real files area.
- Results: all packages OK on Windows; Linux (WSL) uploads and files PASS; golangci-lint 0 issues (windows + linux).

### E099 | 17:34 | PLAN S01.4-T03 finalize (write-ahead, drafted while T02's CI runs)
- `PreFinishResponseCallback` (it runs when the last byte arrives, for PATCH and for creation-with-upload, before the answer):
  1. Load the session. If it has `sha256`, hash the data file and compare; a mismatch → 400 problem, and the upload is removed (data, `.info`, session).
  2. fsync the data file.
  3. `Target.CommitUpload(owner, path, dataPath, size, policy)` → the item.
  4. Remove the `.info` file and the session row.
  5. Answer with the header `Item-Path` (the final path; with `rename` it may differ from `target_path`).
  Any finalize error removes the upload and is answered as a problem: the upload is complete, and no API lets the client finalize it again with another policy.
- `files.Local.CommitUpload`:
  - The target checks as in `CheckUpload` (free space not needed for a rename).
  - `storage.Resolver.MoveInto` renames the data file into the target folder under a hidden temporary name. `internal/storage` is the only layer that may use paths; the parents were checked for links just before.
  - Then `commitFile` under the folder lock (conflict policy, Link or Rename) and `syncFolder`.
  - A rename across file systems is reported as `files.ErrNotSameDevice` (EXDEV / ERROR_NOT_SAME_DEVICE). `uploads` then streams the data file through `files.Upload` (copy + fsync + commit, with the free-space check). This detects the plan's "health check reported a mismatch" case at the rename itself, so no mode switch has to be passed along.
- Fault injection: `uploads` gets a test-only hook called at "verified" (before the commit) and "committed" (after it).
  - A crash at "verified" must leave nothing visible in `files/`.
  - A crash at "committed" must leave the complete file.
  - A checksum mismatch removes the data.
- `TestConflictMatrix` gains the tus finalize rows (`CommitUpload` from a file outside the area), as noted in CURRENT_STATE.

### E100 | 17:41 | TASK DONE S01.4-T03 (finalize)
- S01.4-T02: CI run 35999796296 green; merged into `develop` (3bfdadc) and pushed. Branch `feat/S01.4-T03-finalize`.
- Code: `storage.Resolver.MoveInto` + `ErrNotSameDevice` + `isCrossDevice` (unix EXDEV, windows ERROR_NOT_SAME_DEVICE); `files.Local.CommitUpload` (target checks, MoveInto to a hidden temporary name, the shared `commitFile` under the folder lock, `syncFolder`) and `files.ErrNotSameDevice`; `internal/uploads/finalize.go` (`beforeFinish` as tusd's PreFinishResponseCallback, `finalize`, `syncAndVerify`, `copyIn`, `remove`, `ItemPathHeader`, the `finalizeFault` test hook); `Target` gains CommitUpload and Upload.
- Tests:
  - uploads **`TestFinalizePolicies`**: rename → `/taken (1).txt`; overwrite; fail; a target that appears after creation → 409, the upload removed, the other file untouched, no temporary file.
  - **`TestFinalizeChecksum`**: a matching SHA-256 passes; a mismatch (also given in upper case) → 400 and the data removed.
  - `TestFinalizeInOneRequest`: creation-with-upload and an empty upload.
  - **`TestFinalizeFaults`**: a crash at "verified" leaves nothing in the files area; a crash at "committed" leaves the complete file; no temporary files either way.
  - **`TestFinalizeCopyFallback`**: a not-same-device rename is followed by the copy.
  - The resume test now checks the finished file, `Item-Path`, and that the store and session are empty.
  - storage `TestMoveInto`, `TestIsCrossDevice`; files `TestConflictMatrix` + 9 tus finalize rows (81 cases).
- Results: all packages OK on Windows; Linux (WSL) uploads, files, storage PASS; FuzzAPI 45 s PASS (it can now complete uploads end to end); golangci-lint 0 issues (windows + linux); coverage 90.8%.

### E101 | 17:42 | PLAN S01.4-T04 memory bound (write-ahead, drafted while T03's CI runs)
- `internal/api` `TestMemoryBound` runs through a real server and client:
  - a simple PUT upload and a tus upload (created, then `uploads.max_chunk_size` chunks), each streamed from a generated reader;
  - a copy of the uploaded file;
  - a download of a **sparse** file of the same size (made with truncate, so it costs no disk).
  A goroutine samples the heap (`runtime/metrics` `/memory/classes/heap/objects:bytes`) every 20 ms; the growth over a baseline taken after a GC must stay under 256 MB for each transfer.
- Size: the environment variable `LOCALAINAS_MEMTEST_SIZE` (bytes or with a unit); without it the test is skipped, so the normal suite stays fast.
  - CI gets a separate `memory` job: Linux 10 GiB (as the task says), Windows 1 GiB (sparse files differ there). It prints `df` first.
  - The uploads and the copy write real bytes. If the Linux runner's disk cannot hold 10 GiB, the job will fail with a disk error, and I will report that rather than shrink the size silently.
- The test itself is the evidence that nothing buffers whole files; the streaming paths are already fixed-buffer: 1 MiB for uploads and copies, `http.ServeContent` with a 32 KiB buffer for downloads.

### E102 | 17:45 | PLAN S01.4-T05 and T06 (write-ahead)
- **T05 (size limits):** most of it exists: `uploads.max_file_size` for the simple upload (413 before reading, S01.3-T05) and as tusd's `MaxSize` (413 at creation, S01.4-T02 test), and `uploads.max_chunk_size` as the tus route's body limit.
  - The gap: a tus request that *declares* a body over the chunk limit would have its first bytes stored before the limit cut it off. The new `chunkLimit` wrapper answers 413 before tusd reads anything (`TestTusChunkLimit`: the offset stays 0).
  - A body without Content-Length is still cut at the limit by the route; documented.
- **T06 (abandoned-upload cleanup):**
  - A new setting `uploads.expiry` (default 24h).
  - `uploads.Server.Cleanup(ctx)` removes sessions whose `expires_at` has passed *and* whose data has not been written for the expiry time, so an upload that is still moving is kept. It also removes tusd files that have no session and are older than the expiry (left by a crash).
  - `files.Local.CleanTemp(ctx, olderThan)` removes `.local-ai-nas-tmp-*` files and folders older than that from the files area: simple uploads and copies cut off by a crash, and the note from S01.3-T05. It walks with `io/fs.WalkDir` over `os.Root.FS()`.
  - A small `Scheduler` interface (`internal/schedule`, a ticker in S01, to be replaced by the S04.3 job system) runs both at startup and then every hour.
  - Tests use an injected clock and file times: expired sessions go, active ones stay.

### E103 | 17:47 | TASK DONE S01.4-T04 (memory bound)
- S01.4-T03: CI run 36000680420 green; merged into `develop` (ea7bf6f) and pushed. Branch `feat/S01.4-T04-memory` (only the T04 files are committed; the T05 and T06 drafts stay in the working tree).
- Code: `internal/api/memory_test.go` (`TestMemoryBound`, `heapSampler` on `runtime/metrics`, `zeros`, `b64`); a CI job `memory` (Linux 10 GiB, Windows 1 GiB, `df` printed first); `docs/testing.md` (the command, the job, the link helpers).
- Result on the Windows PC with 1 GiB: the heap grew by at most **1.6 MiB** (simple upload), **1.3 MiB** (tus, 64 MiB chunks), **1.0 MiB** (copy), and **0.0 MiB** (download of a sparse file); 4.2 s. The 10 GiB Linux run is the CI job's; its result is checked before the merge.

### E104 | 17:53 | TASK DONE S01.4-T05 (size limits)
- S01.4-T04: CI run 36001374478 green, including the new `memory` job with 10 GiB on Linux and 1 GiB on Windows (heap growth under 256 MiB for the simple upload, tus, copy, and download); merged into `develop` (ff924f7). Branch `feat/S01.4-T05-limits`.
- Code: `api.chunkLimit` (a tus request whose `Content-Length` is over `uploads.max_chunk_size` → 413 too_large before tusd reads it); the tus routes use it.
- Tests: **`TestTusChunkLimit`** (a 2 KiB chunk over a 1 KiB limit → 413 problem, the offset stays 0, then 1 KiB chunks finish the upload). Already covered: the simple upload over `max_file_size` (S01.3-T05, `TestUploadOverHTTP`: 413 before any byte with `Expect: 100-continue`), and tus `Upload-Length` over `max_file_size` (S01.4-T02, `TestCreateChecksTarget`: 413, nothing stored).
- Docs: conventions.md tus limits; stage row.

### E105 | 17:59 | TASK DONE S01.4-T06 (abandoned-upload cleanup) + S01.4 CLOSED
- S01.4-T05: CI run 36002021933 green; merged into `develop`. Branch `feat/S01.4-T06-cleanup`.
- Code:
  - config: `uploads.expiry` (default 24h, at least 1m; example config and a validation test).
  - uploads: `Index.Expired`, `Server.Cleanup` (expired and idle sessions; orphaned files), `removeFiles`.
  - files: `Local.CleanTemp` (walks with `io/fs.WalkDir` over `os.Root.FS()`).
  - `internal/schedule`: `Scheduler`, `Ticker`.
  - serve: the cleanup runs at startup and hourly and stops on shutdown; the tus server gets the expiry.
- Tests: **`TestCleanup`**, with an injected clock and file times:
  - an expired idle upload is removed (its data, `.info` file, and session; HEAD → 404);
  - an expired upload written to a minute ago is kept, and so is an unexpired one;
  - an old orphan is removed and a new one kept.
  `TestCleanTemp` (old temporary files and folders removed, new ones and items kept), `TestTickerRunsUntilCancelled`.
- S01.4 closure test: **`TestResumeAtAnyPoint`** (one upload cut at 1 B, 1000 B, 256 KiB + 3, 1 MiB, and one byte before the end; each time HEAD gives exactly the bytes sent and the upload resumes from there; nothing appears in the files area until it finishes; the result is byte-identical). It passes 2× on Windows and 3× on Linux.
- Docs: conventions.md (expiry and cleanup); S01 change log rows (T06 details; **S01.4 closed** with all 5 criteria); plan.md S01.4 status Done.
- Results: all packages OK on Windows; Linux (WSL) uploads, files, schedule, api PASS; golangci-lint 0 issues (windows + linux); coverage 90.5%.

### E106 | 18:01 | PLAN S01.5-T05 spec complete + drift check (write-ahead, drafted while T06's CI runs)
- Most of T05 already exists: the spec-first pipeline since S01.5-T04 (oapi-codegen v2.8.0 → `internal/api/gen`; runtime v1.7.0; strict server, so a missing operation does not compile), a CI step that regenerates and fails on a diff, and the spec-to-route test.
- New evidence of completeness: **`TestErrorStatusesAreInTheSpec`** checks, for every case in the contract tables, that the spec declares that status for that operation (or, for 405, for its path). The spec gets what is missing:
  - the tus operations' statuses (the target checks' 404/409/507, tusd's 412/423, finalize errors);
  - the headers `Location`, `Upload-Offset`, and `Item-Path`.
- **Compile check:** removing one operation's method from `server` must break the build (checked once locally and recorded).
- **Drift check in CI, verified once:**
  1. A commit changes the spec without regenerating.
  2. The CI `checks` job must fail at "Generated code is up to date".
  3. The next commit regenerates, and CI turns green.
  Both commits stay in the branch history; no branch is deleted. (The same way S01.1-T05 proved CI turns red.)

### E107 | 18:11 | TASK DONE S01.5-T05 (spec complete + drift check)
- Code and spec:
  - `TestErrorStatusesAreInTheSpec`: every contract case's status is declared for its operation, or, for 405, for its path.
  - `TestTusStatusesAreInTheSpec`: 12 tus requests against a real tus server; each status is declared and each error is a problem.
  - The tus operations in `api/openapi.yaml` complete: statuses 404/405/409/412/423/507 where they occur; the headers `Location`, `Upload-Offset`, `Upload-Length`, `Item-Path`; a `Locked` response.
  - conventions.md review row for the tus endpoint, which S01.4 had left as a placeholder (my omission, fixed now).
- **Mutation checks:**
  - Removing `411` from `createUpload` in the spec made `TestTusStatusesAreInTheSpec` fail, naming the status.
  - Renaming `server.CopyItem` stopped the build ("*server does not implement gen.StrictServerInterface (missing method CopyItem)").
  Both were restored.
- **Drift check verified in CI:**
  1. A deliberate drift commit (the spec changed without regenerating) made the CI run 36003431640 fail at the checks job ("Generated code is up to date").
  2. The next commit regenerated, and CI turned green.

### E108 | 18:12 | PLAN S01.5-T06 offline API docs (write-ahead, drafted while T05's last CI runs)
- Vendor **Redoc 2.5.4**:
  - Take `bundles/redoc.standalone.js` from the npm tarball `redoc-2.5.4.tgz`. The tarball matched npm's published integrity (sha512-M6jWhG1q…, sha1 c2b77b36…). The bundle's SHA-256 is dcaf7661…500b, 1,103,471 bytes.
  - Put it in `internal/api/docs/`, with Redoc's LICENSE (MIT) and the bundle's third-party license file.
  - Register the checksum in `dependencies.md` (R6).
- **The spec is embedded** through a tiny package next to it (`api/spec.go`, `package apispec`, `//go:embed openapi.yaml`); `go:embed` cannot reach outside a package's folder, and this keeps one copy of the spec.
- **Routes:**
  - `/api/docs/` serves the page (`index.html`: `<redoc spec-url="openapi.yaml">` and `<script src="redoc.standalone.js">`, relative URLs only), the bundle, and the spec, with GET/HEAD only (else 405), and 404 for other names.
  - `/api/docs` redirects to `/api/docs/`.
  - Both are method-less catch-alls, so the spec-to-route test does not ask for spec operations; the contract test needs an error case for each.
  - A `Content-Security-Policy` limits the page to its own origin.
- **Tests:**
  - The page contains no absolute URL at all.
  - The bundle's SHA-256 equals the registered one.
  - The served spec equals `api/openapi.yaml` and is valid OpenAPI.
  - 405, 404, and the redirect.
- **"Renders with the network disabled":** Microsoft Edge headless (on this PC) with `--host-resolver-rules` mapping every host but 127.0.0.1 to NOTFOUND, dumping the DOM after the scripts ran. The rendered operation titles must appear. It needs a browser, so it is a script (`scripts/check-api-docs-offline.sh`), not a CI test; the result is recorded here.

### E109 | 21:34 | USER + TASK DONE S01.5-T06 (offline API docs) + S01.5 CLOSED
- User message: "continue".
- S01.5-T05: CI run 36004028905 green (after the deliberate drift run 36003431640 failed as intended); merged into `develop` (04b7f5a). Branch `feat/S01.5-T06-docs`.
- Code:
  - `api/spec.go` (`package apispec`, embeds `openapi.yaml`);
  - `internal/api/docs/` (index.html with relative URLs only, `redoc.standalone.js`, `REDOC-LICENSE`, `redoc.standalone.js.LICENSE.txt`; kept byte for byte by `.gitattributes`);
  - `internal/api/docs.go` (`apiDocs`: GET/HEAD, 405, 404, CSP, `RedocSHA256`; `docsRedirect`) and routes `/api/docs/` and `/api/docs`;
  - `scripts/check-api-docs-offline.sh`;
  - `uploads.knownIDs`/`isUploadID`: foreign upload IDs → 404 problem with `Tus-Resumable`.
- Tests: **`TestAPIDocs`** (the page has no absolute URL and references only the bundle and the spec; the bundle's SHA-256 is the registered one; the served spec equals the file and is valid OpenAPI; CSP; the redirect; HEAD), 3 contract cases, `TestForeignIDsAreNotFound` (8 IDs × HEAD/PATCH/DELETE → 404 problems).
- **Offline render:** `scripts/check-api-docs-offline.sh` with headless Microsoft Edge on this PC, with every host but 127.0.0.1 unresolvable. The rendered DOM showed "local-ai-nas API", "Create a folder", "Download a file", "Upload a file (simple, streamed)", and "Create a resumable upload (tus)": **it renders offline**.
- **FuzzAPI finding:** `HEAD /api/v1/files/uploads/"` answered 500 on Windows, because tusd opens the ID as a file name. Fixed by refusing IDs the server cannot have made; the input stays as a fuzz seed; FuzzAPI 45 s PASS after the fix.
- Docs: `dependencies.md` Redoc row (source, npm integrity, SHA-256, licenses); conventions.md "Documentation"; README link to `/api/docs/`; S01 change log rows (T06 details; **S01.5 closed** with all 5 criteria); plan.md S01.5 status Done.
- Results: all packages OK on Windows; Linux (WSL) api and uploads PASS; golangci-lint 0 issues (windows + linux).

### E110 | 21:41 | TASK START S01.7-T01 integration suite (plan)
- S01.5-T06 + S01.5 closure: CI run 36028388074 green; merged into `develop` (61a09d7). **S01.1–S01.6 are Done.** S01.7 starts; branch `feat/S01.7-T01-integration`.
- Q18 (library size) is needed for S01.7-T04 only; I will ask the user before T04, so the question does not hold up T01–T03.
- Design (`cmd/local-ai-nas/integration_test.go`):
  - The **real binary** is started as a separate process, twice:
    - a main server with small limits (`uploads.max_file_size` 1 MiB, `uploads.max_chunk_size` 64 KiB, `copy.sync_max_bytes` 1 MiB);
    - a "full disk" server with a free-space reserve of 1,000,000 TiB, for 507 and the failing health check (503).
  - Real HTTP clients drive a table of requests; each has the operation and the status it must get.
  - **The spec is the checklist:** every status the spec declares for every operation must be reached by some request, and every error body must validate against the Problem schema.
  - A short, commented list exempts what cannot be caused on purpose from outside, each with its reason and where it is tested instead: 500 (the internal-error paths, unit-tested with injected faults), 423 (tus lock contention), 503 on a tus shutdown, 501 of an unconfigured tus server, 409 of a file replaced during a download (race), and 507 in the tus copy fallback (needs two file systems).

### E111 | 21:44 | TASK DONE S01.7-T01 (integration suite)
- Code: `cmd/local-ai-nas/integration_test.go`: `startLive` (the program as a separate process, like the smoke test), `harness.expect` (a real HTTP request, the status, problem-schema validation, coverage by operation), `notCaused` (7 statuses with reasons), and **`TestIntegration`**.
  - Two servers: small limits, and an impossible free-space reserve.
  - Requests cover every endpoint: health, photos, folders, simple upload, download (ranges and conditions), items, delete, rename, move, copy (including 422 and 507), and tus (create, HEAD, PATCH to finalize with `Item-Path`, terminate, and every error).
  - **Result: 81 operation statuses reached over HTTP; every other declared status is listed in `notCaused`.**
- **Found:** the spec declared 412 for the tus HEAD, but tusd skips its `Tus-Resumable` check for HEAD and GET (unrouted_handler.go:271); removed from the spec. The health check's 503 is a HealthReport by design; the harness validates it against that schema.
- Results: the integration test PASSES on Windows (0.34 s) and on Linux (WSL, 0.18 s); golangci-lint 0 issues (windows + linux); the other api tests still pass.

### E112 | 21:44 | PLAN S01.7-T02 attack suite (write-ahead, drafted while T01's CI runs)
- The S01.6 attack corpus moves from `internal/storage/attack_test.go` to `testutil` (`AttackPaths`, `WindowsAttackPaths`), so the resolver tests, FuzzResolve's seeds, and the API suite share one list.
- `internal/api/attack_test.go`, over real HTTP with a real tus server:
  1. **Traversal:** every corpus path against every path-taking endpoint (list, details, download, upload, folder, rename, move, copy from and to, delete, tus `target_path`). Every answer must be 4xx, nothing may come back from outside, and nothing outside the namespace may change (the S01.3 confinement test's snapshot, now with the whole corpus).
  2. **Malicious names** in every creating endpoint (folder, upload, rename `new_name`, move and copy targets, tus): device names, forbidden and control characters, trailing dots and spaces, too long, look-alikes, the temp prefix → `invalid_name` with a rule.
  3. **Header injection through file names:** a file with CR/LF, quotes, and `;` in its name (made on disk on Linux; Windows cannot hold such names) is downloaded. No injected header may appear, and `Content-Disposition` must carry no raw control character.
  4. **Oversized inputs:** a 1 MiB path, a 300-byte name, a 4097-byte path, 10,000 segments, a 2 MiB JSON body, oversized headers (Go's own 431, the server keeps running), and a huge `Upload-Metadata`.
  5. **tus metadata abuse:** invalid base64, a missing or duplicated `target_path`, a NUL or traversal in it, bad `on_conflict` and `sha256`, and negative or non-numeric `Upload-Length`/`Upload-Offset` → 4xx problems, and nothing stored.

### E113 | 21:50 | TASK DONE S01.7-T02 (attack suite)
- S01.7-T01: CI run 36029417608 green; merged into `develop` (189e57b). Branch `feat/S01.7-T02-attacks`.
- Code:
  - `internal/testutil/attack.go`: the corpus, moved from the storage tests unchanged.
  - `TestNoOperationLeavesTheNamespace` now aims the whole corpus (plus the Windows list on Windows) at all 10 operations and the tus `target_path`.
  - `internal/api/attack_test.go`:
    - **`TestAttackMaliciousNames`**: 27 names × 6 creating endpoints → `invalid_name` with the exact rule (or `outside_root` for the dot look-alike); nothing created.
    - **`TestAttackHeaderInjection`**: names with `;,=`, and on Linux CR/LF and quote injections, are downloaded without any injected header; Content-Disposition carries no line break and parses back to the exact name; the listing stays valid JSON.
    - **`TestAttackOversizedInputs`**: a 300-byte name, 10,000 segments, and a 4,221-byte path on four endpoints → 4xx problems; a 2 MiB body → 413; a 2 MiB header → Go's 431; 500 KiB of tus metadata → 400; the server still answers.
    - **`TestAttackTusMetadata`**: 14 abusive creations (invalid base64, traversal, NUL, relative, policy injection, a bad checksum, negative, non-numeric, or overflowing lengths, both lengths, concatenation, an old tus version, the photos area) and 5 abusive PATCHes → 4xx problems; nothing stored.
- **Fixes the suite forced:**
  1. On **Linux**, GET, download, and DELETE of a path with a segment over 255 bytes answered **500** (`ENAMETOOLONG` mapped to internal). `storage.IsNotFound` now covers `ENAMETOOLONG` and Windows' `ERROR_FILENAME_EXCED_RANGE`, with a test.
  2. On **Windows**, `a:b` was refused by `IsLocal` with no rule. The resolver now reports `forbidden_character` for forbidden characters there, sharing `forbiddenChars` with the name rules.
- Results: all packages OK on Windows; Linux (WSL) api, storage, files PASS (the Linux-only 500s are gone); FuzzResolve 20 s and FuzzAPI 30 s PASS; golangci-lint 0 issues (windows + linux).

### E114 | 21:51 | PLAN S01.7-T03 edge cases (write-ahead, drafted while T02's CI runs)
- `internal/api/edge_test.go`, all through the real HTTP stack:
  - **Unicode:** a name sent decomposed (NFD, as macOS clients do) is stored and listed composed (NFC); it is reachable by either form. Emoji (including ZWJ sequences), Hebrew and Arabic (right-to-left), and combining marks round-trip through upload, listing, download (`Content-Disposition`), rename, copy, and delete.
  - **Empty files:** simple and tus uploads of 0 bytes, download (200, Content-Length 0; a Range on an empty file gets 200 or 416, as ServeContent decides, never 5xx), copy, rename, an empty folder listing.
  - **Sparse multi-GB file (Linux only):** a 5 GiB sparse file gets details (size), HEAD, a range at the very end, and a delete. It is skipped on Windows, where a sparse file is not what truncate makes. The 10 GiB streaming case is already covered by the memory job (S01.4-T04).
  - **Deep nesting:** a folder 100 levels deep made with `parents`, a file at the bottom, listing, download, a move and a copy of the whole tree, and a recursive delete.
  - **10,000 entries:** a folder of 10,000 files listed through the API in pages of 1,000, with no duplicates or gaps, then deleted recursively.

### E115 | 21:55 | TASK DONE S01.7-T03 (edge cases)
- S01.7-T02: CI run 36030178058 green; merged into `develop`. Branch `feat/S01.7-T03-edges`.
- Code: `internal/api/edge_test.go`:
  - **`TestEdgeUnicode`** (7 names: NFD, emoji with a ZWJ family, Hebrew, Arabic, a ligature, stacked combining marks, Japanese). Each is uploaded; stored and listed as NFC; downloaded by its NFD, NFC, and sent forms with `Content-Disposition` giving the NFC name; a re-upload in NFD is a conflict; then copied, renamed, and deleted.
  - **`TestEdgeEmpty`**: simple and tus uploads of 0 bytes, download, four ranges, copy, and an empty folder listed and deleted.
  - **`TestEdgeSparseFile`**: 5 GiB, Linux only, skipped on Windows with its reason. Details, the last 3 bytes by a suffix range, HEAD with the Content-Length, delete.
  - **`TestEdgeDeepNesting`**: 100 levels with `parents`, a file at the bottom, list, move, copy, recursive delete.
  - **`TestEdgeLargeFolder`**: 10,000 files paged in 1,000s, exact order, no duplicates, one recursive delete; skipped with `-short`.
- **Found:** `http.ServeContent` answers `Range: bytes=-1` (or `-0`) on an empty file with 206 and `Content-Range: bytes 0--1/0`, which is invalid (reproduced in a separate program). `fileContent` now drops `Range` for an empty file, so the answer is 200 with no Content-Range; the test asserts exactly that.
- Results: api OK on Windows (the sparse test skipped there) and on Linux (WSL, all 5 PASS, including the sparse file); golangci-lint 0 issues (windows + linux).

### E116 | 22:47 | USER ANSWER Q18 + plan 1.1.2 + TASK START S01.7-T04
- S01.7-T03: CI run 36030776899 green; merged into `develop` (b080a71).
- Asked the user Q18 (library size) before the performance baseline, with three options. User answer: **"100k photos + 100k files (Recommended)"**.
- Plan **1.1.1 → 1.1.2** (PATCH, R4): 1.1.1 archived as `archive/plan-history/plan_v1.1.1.md`. Q18 is recorded as answered (A12 confirmed), with NFR-003's note, the ★ list (none left for S01), and the S01.7 note updated; S01.7 is In Progress; revision entry added. No targets changed.
- Branch `feat/S01.7-T04-perf`; S01.7-T04 In Progress.

### E117 | 22:57 | TASK DONE S01.7-T04 (performance baseline)
- Code:
  - `internal/api/perf_test.go` (`TestPerfBaseline`, opt-in with `LOCALAINAS_PERF_SIZE`; results to `LOCALAINAS_PERF_OUT`): listing p95 over HTTP; raw disk write+fsync, raw read, and raw loopback TCP baselines; simple upload, download, and tus; each the median of 3.
  - `internal/files/bench_test.go` (`BenchmarkListLargeFolder`).
  - `scripts/perf-baseline.sh`.
  - `ReadFrom` pass-through in `logging.recorder` and `api.problemStatus` (+ `TestRecorderReadFrom`).
- **Method corrected along the way:**
  1. The first run used Go's default 4 KiB transport buffers in the client and compared against single runs. The raw write went from 1,906 to 310 MB/s between two runs (disk cache state), which even made tus look like 138% of the raw disk.
  2. Now: 1 MiB client buffers, the median of three runs, and a loopback TCP baseline, since NFR-003 says "disk or network, whichever is slower".
- **Results:**
  - **Windows PC** (Ryzen 5 7640HS, NVMe): listing p95 35/44/36 ms (**met**); upload 406 MB/s = 63% of the raw disk, download 532 MB/s = 35% of the cached read, tus 400 MB/s = 62% (**not met**).
  - **Linux** (WSL2, tmpfs): listing p95 30–40 ms (met); upload 79%, download 130% via sendfile (met), tus 52%.
  - Heap: met (CI memory job at 10 GiB).
  - Pi and mini-PC: not available; the script is the procedure for them.
- The throughput shortfall is **recorded as a deviation for the user's review** at the S01 sign-off (`docs/perf/S01-baseline.md`, S01 change log), with the reasoning (on loopback with NVMe, the HTTP per-byte CPU work shows; the absolute rates are 3–50× gigabit Ethernet) and the next steps.

### E118 | 23:05 | TASK START S01.7-T05 (documentation) + PLAN
- S01.7-T04: CI run 36037945955 green; merged into `develop` (b1e8c3e). Branch `feat/S01.7-T05-docs`.
- **Checked before writing:** every command of the planned guide was run against a live server (`serve` on 127.0.0.1:18080): in Git Bash with curl 8.21, and in Windows PowerShell 5.1 with the Windows `curl.exe` 8.21. This covered folders, uploads, listing, download with a range and HEAD, rename, move, copy, delete, errors, and a tus upload cut off by `--max-time` and resumed with `-C <offset>`. The SHA-256 of the downloaded file matched in both shells.
- **Found:**
  1. In Windows PowerShell 5.1, a JSON argument to `curl.exe` loses its quotes.
  2. Piping JSON to `curl.exe` depends on the console's input encoding (here it added a BOM, which Go's decoder refuses).
  3. The fix that works: a small `Send-Json` helper that writes `ConvertTo-Json` output to a temporary UTF-8 file (no BOM) and sends it with `--data-binary @file`. Tested with a Unicode path.
  4. Git Bash rewrites `path=/x` arguments into Windows paths (`MSYS_NO_PATHCONV=1` stops it).
  5. Paths with spaces or non-ASCII characters must be percent-encoded: `-G --data-urlencode` for GET and DELETE, `--url-query` (curl ≥ 7.87) for `-T` uploads, and `[uri]::EscapeDataString` in PowerShell. All tested.
- **Plan:**
  - New `docs/api/usage.md`: every step in both shells, plus the notes above.
  - README: status line, a short "Use the API" part linking to the guide, and the performance row in the checks table.
  - `docs/README.md` (index; drop the stale "catalogue comes in S01.5"), `scripts/README.md` (list the scripts).
  - `docs/api/conventions.md`: how to resume (HEAD for the offset).
  - Stage T05 → Done; CURRENT_STATE.
  - No plan edit: its statuses are current (S01.7 In Progress until T08).
  - The demo scripts (T06) will be added to the guide in T06.

### E119 | 23:09 | TASK DONE S01.7-T05 (documentation)
- **New `docs/api/usage.md`:** every S01 operation, first with curl for Linux and macOS, then for Windows PowerShell:
  - checking the server, creating folders, uploading with each `on_conflict`, and listing with paging and sorting;
  - downloading, including a range, HEAD, and resuming with `-C -`;
  - rename, move, copy, and delete, including a `recursive` delete;
  - a tus upload, created, cut off, resumed with `HEAD` and `-C <offset>`, and verified by SHA-256; cancelling an upload;
  - errors, and names with spaces or non-ASCII characters.
  - Notes for PowerShell (`curl.exe`; the `Send-Json` helper; the output encoding; `.ps1` files saved with a BOM) and for Git Bash (`MSYS_NO_PATHCONV`; non-ASCII arguments).
- **Verified by running the guide itself:** a script extracted the guide's code blocks.
  - The 14 `sh` blocks ran on Linux (WSL2, curl 8.18) against a Linux build of the server.
  - The 14 PowerShell blocks ran in Windows PowerShell 5.1 (curl.exe 8.21) against the Windows build.
  - Both were green end to end: tus checksums equal, and `Café.txt` uploaded and deleted.
- **Other documents:**
  - README: the status line now says the Files area works through the API; a new "Use the API" part links to the guide; the performance baseline row was added, and the CI sentence now excludes it.
  - `docs/README.md`: usage.md added; the stale errors.md line fixed.
  - `scripts/README.md`: a table of the scripts.
  - `conventions.md`: a link to the guide and a "Resuming" bullet for tus.
  - Plan statuses were already current, so no plan edit.
- **Noted for S01.7-T06:** a client that drops a tus PATCH makes the server log tusd's `BodyReadError` (unexpected EOF) at ERROR level. A dropped connection is normal for tus, so the level is worth checking when the demo scripts make that happen.
